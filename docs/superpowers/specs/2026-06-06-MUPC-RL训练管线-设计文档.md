# MUPC 强化学习模型训练管线 — 技术设计文档

| 版本 | 日期 | 作者 | 状态 |
|------|------|------|------|
| v3.1 | 2026-06-21 | 架构师 | `[DESIGN_APPROVED]` — 合并 v3.0 预测增强架构设计 (原独立文档已融入) |
| v3.0 | 2026-06-21 | 架构师 | `[DESIGN_APPROVED]` |
| v2.18 | 2026-06-17 | 架构师 | `[DESIGN_APPROVED]` |
| v2.17 | 2026-06-17 | 架构师 | `[DESIGN_APPROVED]` |
| v2.16 | 2026-06-15 | 架构师 | `[DESIGN_APPROVED]` |
| v2.15 | 2026-06-14 | 架构师 | `[DESIGN_APPROVED]` |
| v2.14 | 2026-06-14 | 架构师 | `[DESIGN_APPROVED]` |
| v2.13 | 2026-06-14 | 架构师 | `[DESIGN_APPROVED]` |
| v2.12 | 2026-06-14 | 架构师 | `[DESIGN_APPROVED]` |

**对应 PRD:** `docs/superpowers/specs/2026-06-06-MUPC-RL训练管线-PRD.md` v3.1 (`[REVIEWED: PASS]`)
**对应部署端 PRD:** `docs/MUPC/05-MUPC-AI引擎-PRD.md` v3.1 (`[REVIEWED: PASS]`)
**对应部署端设计:** `docs/MUPC/05-MUPC-AI引擎-设计文档.md` v3.1 (`[DESIGN_APPROVED]`)

> **v3.1 合并说明**：本文档为 MUPC 训练管线唯一权威设计文档。预测增强分层混合架构 (TCN + AdditiveAttention + BiLSTM + 误差修正 + VMD + MSSA) 在 §6~§8 完整描述。旧版独立 v3.0 设计文档已废弃删除。

---

## 目录

1. [模块架构](#1-模块架构)
2. [数据加载与状态合成](#2-数据加载与状态合成)
3. [环境仿真 — 物理模型](#3-环境仿真--物理模型)
4. [环境仿真 — 电压仿真引擎（Grid2Op）](#4-环境仿真--电压仿真引擎grid2op)
5. [环境仿真 — 奖励函数](#5-环境仿真--奖励函数)
6. [LSTM 预测模型 (v3.0 预测增强核心)](#6-lstm-预测模型-v30-预测增强核心)
   - 6.1 [AdditiveAttention 注意力机制](#61-additiveattention-注意力机制)
   - 6.2 [p10p50p90 分位数输出](#62-p10p50p90-分位数输出格式)
   - 6.3 [BiLSTM 双向增强](#63-bilstm-双向增强可选-r2go-no-go-准入)
   - 6.4 [训练配置与 MIC 集成](#64-训练配置与-mic-特征筛选集成)
   - 6.5 [Oracle 后备](#65-oracle-后备)
7. [误差修正 BiLSTM (v3.0 R2)](#7-误差修正-bilstm-v30-r2)
8. [VMD 信号分解 (v3.0 R2)](#8-vmd-信号分解-v30-r2)
9. [动作约束校验](#9-动作约束校验)
10. [RL 训练模块 (含 MSSA 接口)](#10-rl-训练模块-含-v30-mssa-接口)
11. [纯 NumPy PPO 后备](#11-纯-numpy-ppo-后备)
12. [Gymnasium 桩模块](#12-gymnasium-桩模块)
13. [ONNX 模型导出 (v3.0 增强)](#13-onnx-模型导出-v30-增强)
14. [数据交换接口 (v3.0)](#14-数据交换接口-v30)
15. [配置与常量](#15-配置与常量)
16. [错误处理](#16-错误处理)
17. [技术决策记录](#17-技术决策记录)

---

## 1. 模块架构

### 1.1 文件与职责

```
data_loader.py              → SMART-DS + 中国合成 + Data Quality 标签
models/lstm.py              → LSTM + TCN + AdditiveAttention + BiLSTM + QuantileLoss (v3.1)
models/error_correction.py  → 误差修正 BiLSTM 模型 + 训练器 (v3.0 R2)
models/vmd.py               → VMD 变分模态分解包装器 (v3.0 R2)
mupc_env.py                 → 兼容重定向: `from mupc_env.core import MupcEnv`
mupc_env/core.py            → MupcEnv 主环境类 (78/79 维观测 + 2 维动作)
mupc_env/action_validator.py → 4+1 条动作约束 ACT-01~05 (v3.1: ACT-02=10)
mupc_env/observation.py     → 观测构建与归一化
mupc_env/rewards.py         → 5 场景奖励函数
mupc_env/constants.py       → 物理常量 + 权重 + 归一化边界
mupc_env/voltage_sim.py     → VoltageSimulator 降级模型
train.py                    → 训练主入口：MSSA --config 接口 + stdout MAPE
_ppo_core.py                → 纯 NumPy PPO 后备
_gym_stub.py                → Gymnasium 最小替代
export_onnx.py              → LSTM+TCN / RL 策略 / 误差修正 → ONNX (含 metadata_props 11键)
config/config_manager.py    → YAML 配置加载器
config/mupc_env_config.yaml → 环境参数配置文件
tools/                      → 训练辅助工具 (eval_unified, diagnose_mode2, diag_m1)
tests/                      → 单元/集成测试 (test_modes, test_grid2op, record_golden)
mupc_env/grid2op/           → Grid2Op + Pandapower 电压仿真引擎
  ├── numpy_chronics.py
  ├── power_flow.py
  ├── network.py
  └── backend.py
```

### 1.2 模块依赖

```
train.py
  ├── data_loader.py          (NumPy + MIC JSON)
  ├── models/lstm.py           (PyTorch, 必需; v3.0: AdditiveAttention + BiLSTM)
  ├── models/error_correction.py     (PyTorch, 可选 R2)
  ├── models/vmd.py       (NumPy, 可选 R2)
  ├── mupc_env.py             (NumPy + gymnasium/_gym_stub)
  │     ├── mupc_env/action_validator.py (v3.1: ACT-01~05, 2维)
  │     ├── mupc_env/grid2op/       (可选, Grid2Op 电压仿真)
  │     └── _gym_stub.py
  ├── stable_baselines3        (主路径)
  │     └── _ppo_core.py (NumPy only, 后备)
  └── export_onnx.py (含 metadata_props 10 键)
```

### 1.3 R1 vs R2 分轮交付策略

| 轮次 | 交付物 | 必须 |
|------|--------|------|
| R1 | `lstm_attn.onnx` + AdditiveAttention + metadata_props + `--config` CLI + stdout MAPE + MIC JSON | **是** |
| R2 | `bilstm_attn.onnx` + VMD K 通道 + `error_correction.onnx` | 否 |

> **设计原则**：R1 必须项全做，R2 预留接口但功能用 feature flag 控制。所有新功能通过 feature flag (`with_attention`, `output_mode`, `--config`) 控制，不传参数时等价于 v2.18 行为。

### 1.4 训练数据流

```
SMART-DS CSV/Parquet
        │
        ▼
data_loader: 加载光伏/负荷/辐照/温度 + 合成 TOU电价/需量/调度
        │
        ▼
[MIC 离线分析] → mic_features.json → --mic 特征筛选 (v3.0)
        │
        ▼
[VMD 分解] → K 个 IMF 子模态 (v3.0 R2, 可选)
        │
        ▼
LSTM 训练 (离线) → lstm_attn.onnx / bilstm_attn.onnx / error_correction.onnx
        │
        ▼  (LSTM 预测值 或 Oracle 后备)
MupcEnv.reset() → 初始化 SOC + 随机起始位置
        │
        ▼
for each step:
  1. 构建 78/79 维观测向量
  2. 策略网络前向 → 2 维动作 [p_ref, k_droop]
  3. ActionValidator (ACT-01~05) → 约束后动作
  4. 物理仿真: SOC/变压器/电压更新 (Grid2Op 或 VoltageSimulator)
  5. RewardCalculator: 5 场景公式 → 标量奖励
        │
        ▼
train.py PPO/SAC 训练循环
        │
        ▼
export_onnx.py → rl_policy.onnx + lstm_attn.onnx + error_correction.onnx
```

---

## 2. 数据加载与状态合成

### 2.1 SmartDSLoader

```python
class SmartDSLoader:
    PV_ARRAY_KW_REF = 1000.0    # CSV 中光伏参考容量
    PV_ARRAY_KW_TARGET = 150.0  # MUPC 实际光伏容量
    LOAD_PEAK_KW = 60.0
    CONTRACT_DEMAND_KW = 200.0  # v3.1 对齐下游 data_fusion.rs
    GRID_EMISSION_FACTOR = 0.581

    def load_all(self) -> dict[str, np.ndarray]:
        """返回: {'pv_power', 'load_power', 'ghi', 'dni', 'temperature', 'timestamps'}"""
```

光伏缩放: `pv = csv['kW Generated'] * (150 / 1000)`
负荷缩放: `load = per_unit_values * 60`

### 2.2 合成数据生成

```python
class SyntheticDataGenerator:
    """生成部署环境由实时数据源提供的字段。"""

    def generate_tou_prices(self, timestamps: np.ndarray) -> dict:
        """TOU 电价表。
        峰 10:00-12:00, 17:00-21:00 → 1.2 元/kWh, tariff_id=2
        尖峰 12:00-14:00 (夏季6-9月) → 1.5 元/kWh, tariff_id=3
        平 08:00-10:00, 12:00-17:00, 21:00-23:00 → 0.8 元/kWh, tariff_id=1
        谷 23:00-08:00 → 0.4 元/kWh, tariff_id=0
        """

    def generate_demand(self, load_power: np.ndarray) -> dict:
        """需量合成: 4步滑动窗口均值。
        current_demand[i] = max(mean(load_power[i-3:i+1]), contract_demand * 0.3)
        peak_demand_this_month 为当月累计最大值。
        contract_demand = 200 kW (v3.1 对齐下游).
        """

    def generate_dispatch(self, num_steps: int, scene: str) -> dict:
        """调度指令合成。
        非 VPP 场景: 全部为 None (→ 0.0)
        VPP 场景 (MODE-04): 每 96 步随机生成调度事件
          dispatch_p_set ∈ [-200, 200] kW, dispatch_q_set ∈ [-100, 100] kVar.
        """
```

### 2.3 电压仿真（VoltageSimulator 降级模式）

```python
class VoltageSimulator:
    """三相电压简化线路模型（Grid2Op 不可用时的降级方案）。
    物理基础: Q-V 耦合。感性无功吸收 → 电压降低；容性无功释放 → 电压升高。
    """
    K_P = 0.05; K_Q = 0.03; S_BASE = 200.0  # kVA
    V_MIN, V_MAX = 0.85, 1.15
    NOISE_STD = 0.005; PHASE_IMBALANCE = 0.003

    def step(self, p_net: float, q_batt: float,
             prev_va: float, prev_vb: float, prev_vc: float
             ) -> tuple[float, float, float]:
        """p_net: 净有功 = P_pv - P_load_eff + P_batt (kW)."""
        dv = (self.K_P * p_net + self.K_Q * q_batt) / self.S_BASE
        va = prev_va + dv + np.random.normal(0, self.NOISE_STD)
        vb = prev_vb + dv + np.random.normal(0, self.NOISE_STD) + self.PHASE_IMBALANCE
        vc = prev_vc + dv + np.random.normal(0, self.NOISE_STD) - self.PHASE_IMBALANCE
        return (np.clip(va, self.V_MIN, self.V_MAX),
                np.clip(vb, self.V_MIN, self.V_MAX),
                np.clip(vc, self.V_MIN, self.V_MAX))
```

### 2.4 归一化策略

```python
NORM_SCHEME = {
    "battery_soc":      "identity",           # [0,1]
    "pv_power":         "minmax(0, 150)",     # → [0,1]
    "load_power":       "minmax(0, 60)",      # → [0,1]
    "grid_power":       "minmax(-200, 200)",  # → [-1,1]
    "transformer_load": "identity",           # [0,~2]
    "battery_power":    "minmax(-50, 50)",    # → [-1,1]
    "voltage_phase_*":  "minmax(0.85, 1.15)", # → [-1,1]
    "pv_forecast[*]":   "minmax(0, 150)",     # → [0,1]
    "load_forecast[*]": "minmax(0, 60)",      # → [0,1]
    "current_price":    "minmax(0, 1.5)",     # → [0,1]
    "tariff_id":        "minmax(0, 3)",       # → [0,1]
    "current_demand":   "minmax(0, 500)",     # → [0,1]
    "solar_irradiance": "minmax(0, 1500)",    # → [0,1]
    "temperature":      "minmax(-20, 60)",    # → [-1,1]
    "dispatch_p_set":   "minmax(-200, 200)",  # → [-1,1], None→0.0
    "q_realtime_margin":"identity",           # [0,1]
    "mode_id":          "identity",           # [0,1]
}
```

---

## 3. 环境仿真 — 物理模型

### 3.1 MupcEnv 类结构

```python
class MupcEnv(gym.Env):
    """78/79 维观测 (v2.17), 2 维动作 [p_ref, k_droop] (v2.15)."""

    # 物理常量 (完整列表见 §15.1)
    # TRANSFORMER_KVA=200, BATTERY_CAPACITY_KWH=100, P_BATT_MAX_KW=50, SOC=[0.10,0.90], etc.

    # 子模块
    voltage_sim: VoltageSimulator; validator: ActionValidator
    _grid2op_power_flow: Grid2OpPowerFlow | None; _use_grid2op: bool = True
    _predictor: LSTMForecast | OraclePredictor  # v3.0: LSTM 预测接口

    # 内部状态
    soc: float; battery_power_prev: float; load_rate: float
    va, vb, vc: float; peak_demand: float; current_mode: str; step_idx: int
```

### 3.2 to_input_vector() — 78 维序列化

布局严格对齐下游 `FusedSystemState.to_input_vector()`：

| 索引 | 数据段 | 维数 |
|------|--------|------|
| 0~8 | D1 实时数据 (SOC/PV/负荷/电网/变压器/电池/三相电压) | 9 |
| 9~23 | D2 pv_forecast (P50) | 15 |
| 24~38 | D2 load_forecast (P50) | 15 |
| 39~41 | D3 电价 (current/next/tariff_id) | 3 |
| 42~44 | D4 需量 (current/contract/peak) | 3 |
| 45~46 | D5 气象 (irradiance/temperature) | 2 |
| 47 | D6 dispatch_p_set | 1 |
| 48 | D7 q_realtime_margin | 1 |
| 49~56 | D8 季节+时段 one-hot (6+2) | 8 |
| 57~60 | D9 safety_override (active/p_ref/consecutive/ratio) | 4 |
| 61~75 | D10 load_forecast_quantiles (P90) | 15 |
| 76 | D10 shock_load_probability | 1 |
| 77 | D10 base_load | 1 |
| 78* | mode_id (仅多模式) | 1 |

总计: 78 维 (单模式) / 79 维 (多模式)

### 3.3 物理仿真方程

```python
def _physics_step(self, action_clamped):
    # 1. 动作反归一化 (v2.15: 2 维 [p_ref, k_droop])
    p_ref   = action_clamped[0] * 50.0       # [-50, 50] kW
    k_droop = action_clamped[1] * 100.0      # [-100, 100] kW/V
    q_batt  = self._q_batt_realtime          # Q 由实时电压调节器闭环 (不经过 RL)
    load_shedding = 0.0; pv_limit = 1.0      # 下沉至 strategy-engine

    # 2. 有效负荷和光伏
    p_load_eff = max(0, p_load_raw - load_shedding)
    p_pv_eff = p_pv_raw * pv_limit

    # 3. SOC 更新 (SAFETY: hard clamp [10%, 90%])
    soc_raw = self.soc + (-p_ref * 0.25) / 100.0
    soc_new = np.clip(soc_raw, 0.10, 0.90)

    # 4. 电网交换功率 + 变压器负载率
    grid_power = p_load_eff - p_pv_eff + p_ref
    q_load = p_load_eff * tan(acos(0.90))
    s_trafo = sqrt(grid_power**2 + (q_load - q_batt)**2)
    load_rate = s_trafo / 200.0

    # 5. 电压更新 (Grid2Op 或 VoltageSimulator)
    if self._use_grid2op:
        va, vb, vc = self._grid2op_step(p_ref, q_batt, k_droop)
    else:
        p_net = p_pv_eff - p_load_eff + p_ref
        va, vb, vc = self.voltage_sim.step(p_net, q_batt, ...)

    # 6. 下垂控制公式 (v3.1 对齐下游):
    #    P_output = P_ref - k_droop × ΔV
    #    电压偏高 (ΔV>0): P_output↓ → 趋向充电 → 拉低电压 ✓
    #    电压偏低 (ΔV<0): P_output↑ → 趋向放电 → 抬升电压 ✓
```

### 3.4 q_realtime_margin 的训练模拟

```python
# step() 开头: 用上一步末端电压推算 Q-V 闭环
v_prev = (self.va + self.vb + self.vc) / 3.0
q_batt = np.clip(-K_Q_V * (v_prev - 1.0), -300, 300)
self._q_realtime_margin = 1.0 - abs(q_batt) / 300.0
```

训练时模拟，部署时由实时控制模块通过 intercore DataUploadPayload 注入。

---

## 4. 环境仿真 — 电压仿真引擎（Grid2Op）

### 4.1 组合模式

```
MupcEnv (外部: Gymnasium 接口)
  └── Grid2OpPowerFlow (内部: 潮流计算引擎)
        ├── PandapowerNetwork (农网台区拓扑)
        └── NumpyChronics (时序数据注入)
```

### 4.2 NumpyChronics

将 `data` dict (单相标量) 转换为 Grid2Op 三相格式，模拟三相不平衡 ±0.3%。

### 4.3 Grid2OpPowerFlow

```python
class Grid2OpPowerFlow:
    def step(self, storage_p_mw, storage_q_mvar) -> tuple[float, float, float, bool]:
        """执行一步潮流计算, 返回 (va, vb, vc, has_illegal)."""
```

潮流不收敛时回退到上一时刻安全电压。

### 4.4 Pandapower 网络拓扑

**create_mupc_network()**：构建农网台区物理拓扑。

```python
def create_mupc_network() -> "pp.auxiliary.pandapowerNet":
    """构建农网台区 Pandapower 网络。
    拓扑: 外部电网(10kV) → 配电变压器(200kVA,10/0.4kV) → 低压母线
          → 主干线路(NAYY 4x50 SE,1.5km) → 末端节点
            ├── 居民负荷 + 农业冲击负荷
            ├── 屋顶光伏 (150kW)
            └── MUPC 储能 (100kWh)
    """
    import pandapower as pp
    net = pp.create_empty_network(name="Agri_MUPC_LV_Network", f_hz=50.0)
    ext_bus = pp.create_bus(net, vn_kv=10.0, name="HV_Grid")
    pp.create_ext_grid(net, bus=ext_bus, vm_pu=1.0, va_degree=0)
    lv_bus = pp.create_bus(net, vn_kv=0.4, name="LV_Main_Bus")
    pp.create_transformer(net, hv_bus=ext_bus, lv_bus=lv_bus,
                          std_type="0.2 MVA 10/0.4 kV", name="Dist_Transformer")
    end_bus = pp.create_bus(net, vn_kv=0.4, name="End_Node_Bus")
    pp.create_line(net, from_bus=lv_bus, to_bus=end_bus, length_km=1.5,
                   std_type="NAYY 4x50 SE", name="Main_Overhead_Line")
    pp.create_load(net, bus=end_bus, p_mw=0.0, q_mvar=0.0,
                   name="Residential_Load", controllable=False)
    pp.create_load(net, bus=end_bus, p_mw=0.0, q_mvar=0.0,
                   name="Agri_Shock_Load", controllable=False)
    pp.create_sgen(net, bus=end_bus, p_mw=0.0, q_mvar=0.0,
                   name="Rooftop_PV", controllable=True)
    pp.create_storage(net, bus=end_bus, p_mw=0.0, max_e_mwh=0.2,
                      soc_percent=50.0, name="MUPC_BESS", controllable=True)
    return net
```

### 4.5 SOC 双向同步

```
step() 入口: Grid2Op SOC → MupcEnv._soc (同步上一轮结果)
step() 出口: MupcEnv SOC → Grid2Op (写入新 SOC)
```

---

## 5. 环境仿真 — 奖励函数

### 5.1 SCENE-01：台区季节性负荷模式

```
R_agri = w1·R_pv - α(s)·w2·P_batt_deg - w3·P_overload + w4·R_PQ
       - w5·R_ramp - w6·R_vslope - w7·R_smooth - w8·R_safety
       + w9·R_readiness + 0.1·R_safety_margin  (v3.1 Log-barrier)
       + R_shaping + R_soc_balance + R_state_improve
```

**自适应损耗系数 α(s)**：
- SOC < 10%: α=3.0 (极低保护)
- q_margin ≤ 10% 且 |ΔV| > 5% 连续 ≥ 2 步: α=0.2 (电压支撑模式)
- 其他: α=1.0 (常规)

**子项归一化 (对齐下游 v2.12 R-01)**：

| 子项 | 归一化 |
|------|--------|
| r_pv_norm | clip(r_pv / 100, -1, 1) |
| p_batt_deg_norm | clip(-p_batt_deg × 10, -1, 0) |
| p_overload_norm | clip(p_overload / 100, -1, 0) |
| r_pq_norm | clip(r_pq / 50, -1, 1) |
| p_ramp_norm | clip(-p_ramp × 10, -1, 0) |
| p_vs_norm | clip(-r_voltage_slope × 10, -1, 0) |
| r_smooth_norm | clip(r_smooth / 130, -1, 0) |
| r_safety_norm | 已归一化 [-1, 0] |

**P-Q 协同度 (Sigmoid 平滑化)**：
```
w_save = 1/(1+exp(-50·(q_margin-0.10))); w_support = 1-w_save
r_pq = w_save·r_lazy + w_support·r_correct
```

**SafetyOverride 惩罚**：
```python
def _compute_safety_override(active, consecutive, ratio):
    if not active: return 0.0
    if consecutive < 10: return -1.0  # 冷启动, clamp 对齐下游 .max(-1.0)
    return (-5.0*ratio - 10.0*min(consecutive/10, 1)) / 15.0  # [-1, 0]
```

**权重 (MODE-01, v3.1)**：w1=1.0, w2=0.5, w3=2.0, w4=1.0, w5=0.5, w6=0.5, w7=0.5, w8=1.0, w9=1.0

### 5.2 安全边界 Log-barrier 软化 (v3.1 专家评审)

将硬截断的安全边界替换为连续可导的 Log-barrier 函数 `_compute_safety_margin()`：

```
penalty = -Σ log(max(margin_i, ε))
```

监控 4 个边界：电压上限 1.15 / 下限 0.85、变压器负载 0.85、SOC 下限 0.10 / 上限 0.90（SOC 半权重，因已有硬约束仅做预警）。

**参考边界重缩放** (margin/ref >= 1 → 安全, < 1 → 惩罚递增):
| 边界 | 参考值 (ref) | epsilon |
|------|-------------|---------|
| 电压 | 0.10 (10% 裕度) | 0.01 |
| 负载 | 0.20 (20% 裕度) | 0.01 |
| SOC | 0.20 (20% 裕度) | 0.01 |

**典型值**: 安全态(v=1.0,load=0.5,soc=0.5)→0.0; 警告态(v=1.10,load=0.80,soc=0.15)→~-0.55; 危险态→-1.0(饱和)。以 w=0.1 小权重加入总奖励。

### 5.3 Welford 逐分量 EMA 自适应归一化 (v3.1 专家评审)

`core.py` 增强 Welford 系统：对 r_pv, p_overload, r_pq, r_safety, r_smooth, r_safety_margin 6 个关键分量做 EMA 跟踪 (alpha=0.001)。统计量暴露在 `get_welford_stats()` → `component_ema` 字段供 TensorBoard 监控，为后续自适应权重调节奠基。

### 5.2 SCENE-B1：自主套利

```
R_arbitrage = w1·R_spread - w2·P_batt_deg
P_batt_deg = (|p_ref| / 100)^2  (C-rate², v2.15 训练侧)
```
权重: w1=1.0, w2=1.0, w3=2.0 (过载惩罚)

### 5.3 SCENE-B2：需量控制

```
R_demand = w1·R_demand_avoid - w2·P_comfort
```
权重: w1=1.0, w2=0.5

### 5.4 SCENE-B3：虚拟电厂

```
R_vpp = w1·R_ancillary + w2·R_accuracy - w3·P_deadline
```
权重: w1=1.0, w2=2.0, w3=1.0

### 5.5 SCENE-B5：极致绿色

```
R_green = w1·R_green_consumption + w2·R_carbon_reduction
```
权重: w1=1.0, w2=1.0

### 5.6 默认权重汇总

```python
DEFAULT_WEIGHTS = {
    "MODE-01": [1.0, 0.5, 2.0, 1.0, 0.5, 0.5, 0.5, 1.0, 1.0],  # w1~w9
    "MODE-02": [1.0, 1.0, 2.0],      # w1(价差), w2(电池), w3(过载)
    "MODE-03": [1.0, 0.5],           # w1(需量), w2(舒适度)
    "MODE-04": [1.0, 2.0, 1.0],      # w1(辅助), w2(精度), w3(延迟)
    "MODE-05": [1.0, 1.0],           # w1(绿电), w2(碳减排)
}
MODE_ID_MAP = {"MODE-01": 0.0, "MODE-02": 0.25, "MODE-03": 0.5,
               "MODE-04": 0.75, "MODE-05": 1.0}
```

---

## 6. LSTM 预测模型 (v3.0 预测增强核心)

> **v3.0 变更**：LSTM 从简单的"取最后步 hidden + 2 头 Linear"升级为 **LSTM + AdditiveAttention + 6 头分位数输出**。输入窗口从 8 步扩展到 12/24/36 步 (MSSA 搜索)，特征维度从固定 7 维变为 MIC 筛选 K 维。支持双向 LSTM (BiLSTM) 可选增强。  
> **v3.1 TCN**：新增 TCN 时域卷积特征提取层（4层因果膨胀卷积，默认开启），插入在 VMD 与 LSTM 之间，架构升级为 **VMD → TCN → LSTM → Attention → 6头**。

### 6.1 AdditiveAttention 注意力机制

**文件**：`models/lstm.py`

**新增类 `AdditiveAttention`**（全部 ONNX 标准算子）：

```python
class AdditiveAttention(nn.Module):
    """加法注意力 (Bahdanau), 所有算子 ONNX 标准.
    Input:  (B, seq_len, H)  LSTM hidden states
    Output: (B, H)           上下文向量
    """
    def __init__(self, hidden_dim: int):
        self.W = nn.Linear(hidden_dim, hidden_dim)  # Gemm
        self.v = nn.Linear(hidden_dim, 1)           # Gemm

    def forward(self, h: Tensor) -> tuple[Tensor, Tensor]:
        score = self.v(torch.tanh(self.W(h)))   # (B, T, 1): Tanh
        weights = torch.softmax(score, dim=1)   # (B, T, 1): Softmax
        context = torch.sum(weights * h, dim=1) # (B, H):   Mul + ReduceSum
        return context, weights.squeeze(-1)
```

**ONNX 算子验证**（全部 RKNN 支持）：
| 算子 | ONNX op | RKNN |
|------|---------|------|
| Linear | Gemm | ✅ |
| Tanh | Tanh | ✅ |
| Softmax | Softmax | ✅ |
| Mul + ReduceSum | Mul + ReduceSum | ✅ |

**设计决策**：选择 AdditiveAttention 而非 Self-Attention，因为 (1) 24 步短序列下加法注意力足够；(2) 参数量 ~H² 远小于 Self-Attention 的 3H²；(3) 全算子 ONNX 标准，NPU 可直接推理。

### 6.2 p10p50p90 分位数输出格式

**LSTMForecast 新参数**：

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `input_dim` | int | 7 | MIC 筛选后的特征维度 |
| `hidden_dim` | int | 64 | LSTM 隐状态维度 (MSSA 搜索) |
| `num_layers` | int | 2 | LSTM 层数 |
| `input_window` | int | 12 | 输入窗口步数 (12/24/36, MSSA 搜索) |
| `output_mode` | str | "p10p50p90" | "p10p50p90" 三通道 或 "legacy" 双头 |
| `with_attention` | bool | True | 是否含 Attention |
| `attn_score` | str | "additive" | 注意力评分类型 |
| `bidirectional` | bool | False | 是否双向 LSTM (R2) |

**forward 计算图**：

```
x (B, T, K)   K ≤ 7, T ∈ {12, 24, 36}
  → [TCN] (4-layer causal dilated conv, d=[1,2,4,8], default ON) → (B, T, H)
  → LSTM → h_seq (B, T, H)
  → AdditiveAttention(h_seq) → ctx (B, H)
  → 6 个 Linear 头:
      head_pv_p10/head_pv_p50/head_pv_p90 → 各 (B, 15)
      head_load_p10/head_load_p50/head_load_p90 → 各 (B, 15)
  → stack → (B, 2, 15, 3)   # PV/Load × 15步 × (P10, P50, P90)
```

**输出维度对照**：

| 场景 | 输出 shape | 说明 |
|------|-----------|------|
| p10p50p90 (R1) | `(B, 2, 15, 3)` | PV/Load × 15步 × 三通道 |
| 含 VMD (R2) | `(B, K, 2, 15, 3)` | K=模态数 |
| legacy 兼容 | `(B, 47)` | 旧 47 维，过渡期 |

**p10p50p90 → legacy 转换** (`_p10p50p90_to_legacy()`): 提取 P50 通道 → (B, 30) D2 预测 + P90 通道 → (B, 17) D10 预测。

### 6.3 BiLSTM 双向增强（可选 R2, Go/No-Go 准入）

将单向 LSTM 替换为 BiLSTM，前向+后向隐状态拼接后送入 Attention：

```
正向 LSTM: t-T → ... → t      ─┐
反向 LSTM: t → ... → t-T      ─┤→ 拼接 [h_fwd; h_bwd] → Attention
```

**准入条件 (Go/No-Go Gate)**：

| 条件 | 结果 |
|------|------|
| RK3588 实测 ≥1000 次, P99 < 900ms | Go — BiLSTM 可启用 |
| P99 ≥ 900ms | No-Go — 降级至单向 LSTM |

BiLSTM 启用时 hidden_dim 折半以控制参数量 ≤ 单向的 2.2 倍。

### 6.4 训练配置与 MIC 特征筛选集成

```python
LSTM_TRAIN_CONFIG = {
    "input_seq_len": 8,              # legacy: 120 分钟 / 15 分钟
    "input_window": 24,              # v3.0: MSSA 搜索 12/24/36, 默认 24
    "forecast_steps": 15,
    "hidden_dim": 64, "num_layers": 2,
    "dropout": 0.1, "batch_size": 64,
    "learning_rate": 1e-3,
    "epochs": 200,                   # v3.1: 100 → 200
    "patience": 20,                  # v3.1: 15 → 20
    "output_mode": "p10p50p90",
    "with_attention": True,
    "loss": "quantile",              # v3.1: Huber → QuantileLoss
    "quantile_taus": [0.1, 0.5, 0.9],  # v3.1: 分位数目标
    "with_tcn": True,                # v3.1: TCN 默认开启
}

# MIC 集成 (train.py 中)
mic_result = json.load(open(mic_json_path))
selected_features = [f["name"] for f in mic_result["features"] if f["selected"]]
model = LSTMForecast(
    input_dim=len(selected_features),
    input_features=selected_features,
    output_mode="p10p50p90", with_attention=True,
)
```

**prepare_data() 窗口**：`seq_len = cfg.get("input_window", cfg.get("input_seq_len", 8))`

### 6.5 QuantileLoss 真分位数回归（v3.1 默认）

```python
class QuantileLoss:
    """Pinball Loss (分位数损失), v3.1 默认损失函数 (替代 Huber/MSE).
    tau ∈ {0.10, 0.50, 0.90} 对应 P10/P50/P90 分位数。
    下游要求: P90 覆盖率验证 (实际值 ≤ P90 的比例应接近 90%, ±2%).
    """
    def __init__(self, quantiles: list[float] = [0.1, 0.5, 0.9]):
        super().__init__()
        self.quantiles = quantiles

    def __call__(self, pred, target):
        # pred: (B, 2, 15, 3), target: (B, 2, 15, 3)
        losses = []
        for i, q in enumerate(self.quantiles):
            error = target[..., i] - pred[..., i]
            losses.append(np.maximum(q * error, (q - 1) * error))
        return np.mean(np.stack(losses))
```

### 6.6 Oracle 后备

```python
class OraclePredictor:
    """LSTM 不可用时: 真实未来值 + 噪声 (光伏 5%, 负荷 8%)。
    Oracle 永远不提供 D10, _d10_trained_count=0 让 MupcEnv 走 data 合成 fallback。
    """
    def predict(self, step_idx: int) -> np.ndarray:
        """返回 (30,) = [pv_15, load_15] (D2 only, 向后兼容)."""
```

---

## 7. 误差修正 BiLSTM (v3.1 已集成)

**文件**：`models/error_correction.py`。v3.1 已完整接入 train.py + export_onnx.py。

### 7.1 架构

```python
class ErrorCorrectionBiLSTM(nn.Module):
    """独立轻量 BiLSTM, 用于残差修正。
    Input:  (B, 24, 1)  历史残差序列
    Output: (B, 15, 1)  未来残差预测
    """
    def __init__(self, hidden_dim=32, num_layers=1, residual_window=24):
        self.bilstm = nn.LSTM(1, hidden_dim, num_layers,
                              bidirectional=True, batch_first=True)
        self.head = nn.Linear(hidden_dim * 2, 1)

    def forward(self, x):
        h, _ = self.bilstm(x)        # (B, 24, 2H)
        return self.head(h[:, -15:]) # (B, 15, 1)
```

| 属性 | 规格 |
|------|------|
| 输入 | 最近 24 步历史残差 (冷启动时零向量) |
| 输出 | 未来 15 步预测残差 |
| hidden_dim | 32 |
| num_layers | 1 |
| 参数量 | ≤ 主 LSTM 的 50% |
| INT8 模型 | ≤ 3MB |

### 7.2 训练流程

```python
class ErrorCorrectionTrainer:
    def train(self, data, main_model: LSTMForecast) -> dict:
        """5 步训练:
        1. 主模型对训练集做预测 → y_pred_1
        2. 计算残差 e = y_true - y_pred_1 (P50 通道)
        3. 构建残差训练数据: X_err(24步历史残差) → y_err(15步未来残差)
        4. 训练轻量 BiLSTM (MSE loss)
        5. 偏差检查: |bias| < 3% MAPE → 跳过, 不导出模型
        """
```

**启用条件 (Bias Gate)**：`|bias| > 3% MAPE` 才启用误差修正，否则跳过训练且推理端不加载模型。

**推理阶段故障处理**：连续 3 次失败 → 自动禁用；连续 5 次成功 → 自动恢复。

### 7.3 ONNX 导出

```python
def export_error_correction(checkpoint_path, output_dir="./exported_models/",
                            residual_window=24) -> str:
    torch.onnx.export(model, dummy, onnx_path,
        input_names=["residual_history"], output_names=["residual_forecast"],
        metadata_props={
            "mupc_model_type": "error_correction",
            "mupc_residual_window": str(residual_window),
            "mupc_output_horizon": "15", "mupc_version": "v3.1",
        })
```


### 7.4 训练管线集成 (v3.1)

已完整接入 train.py + export_onnx.py:

- CLI: --no-error-correction (默认启用, 约增 1-2 分钟)
- Checkpoint: checkpoints/error_correction.pt
- ONNX 导出: python export_onnx.py --error-correction checkpoints/error_correction.pt
- --export-onnx 时自动连带导出 (如 checkpoint 存在)
- Bias Gate: 偏差 <= 3% MAPE 时自动跳过 (模型为 None)
- MSSA 目标函数自动传入 --no-error-correction

---

## 8. VMD 信号分解 (v3.0 R2)

**新增文件**：`models/vmd.py`

```python
class VmdDecomposer:
    """VMD 变分模态分解包装器。
    训练阶段: 对输入窗口执行 VMD 分解, 各 IMF 分别送入 LSTM。
    部署阶段: 推理端 CPU (Rust + rustfft) 执行, 训练管线不参与。
    K 值由 MSSA 搜索确定, 光伏 4~6, 负荷 5~8。
    """
    def __init__(self, K=5, alpha=2000.0, tau=0.0,
                 tol=1e-6, max_iter=500, dc=False, init_mode=1):

    def decompose(self, signal: np.ndarray) -> np.ndarray:
        """将 (T,) 信号分解为 (K, T) IMF 分量."""

    def decompose_batch(self, X: np.ndarray, feature_idx: int = 0) -> np.ndarray:
        """批次分解: (N, T, D) → (N, K, T)."""

VMD_DEFAULT_CONFIG = {
    "enabled": False, "K": 5, "alpha": 2000.0,
    "tau": 0.0, "tol": 1e-6, "max_iter": 500,
    "dc": False, "init_mode": 1,
}
```

集成到 data_loader：`if cfg.vmd_enabled: imfs = vmd.decompose_batch(X, feature_idx=0)`

---

## 9. 动作约束校验

**文件**：`mupc_env/action_validator.py`

**v3.1 版本**：2 维动作 `[p_ref, k_droop]`，5 条约束 ACT-01~05：

```python
class ActionValidator:
    P_BATT_MAX: float = 50.0              # kW
    K_DROOP_MIN: float = 0.0              # kW/V (v3.1 对齐下游 [0,30])
    K_DROOP_MAX: float = 30.0             # kW/V (v3.1 对齐下游 [0,30])
    DELTA_P_MAX: float = 50.0             # kW/步, ACT-01
    DELTA_K_DROOP_MAX: float = 10.0       # kW/V/步, ACT-02
    MAX_APPARENT_POWER_KVA: float = 200.0  # kVA, ACT-03 S-circle

    def validate(self, action_norm, dispatch_p=None):
        p_ref, k_droop = self._denormalize(action_norm)

        # ACT-01: |Δp_ref| ≤ 50 kW/步
        # ACT-02: |Δk_droop| ≤ 10 kW/V/步
        # ACT-03: √(p_ref² + k_droop²) ≤ 200 kVA (S-circle)
        # ACT-04: k_droop ∈ [-100, 100] kW/V
        # ACT-05: |p_ref| ≤ |dispatch_p| (调度权限)

        # 反归一化:
        #   p_ref   = a[0] × 50.0        → [-50, 50] kW
        #   k_droop = a[1] × 100.0       → [-100, 100] kW/V
```

**约束规则汇总**：

| 规则 | 约束条件 | 越限处理 |
|------|----------|----------|
| ACT-01 | \|Δp_ref\| ≤ 50 kW/步 | clamp |
| ACT-02 | \|Δk_droop\| ≤ 10 kW/V/步 | clamp |
| ACT-03 | √(p_ref² + k_droop²) ≤ 200 kVA | 等比缩放 |
| ACT-04 | k_droop ∈ [-100, 100] kW/V | clamp |
| ACT-05 | \|p_ref\| ≤ \|dispatch_p\| | clamp |

> **v2.15 下沉说明**：load_shedding/pv_limit 约束下沉至 strategy-engine，不再纳入 AI 动作约束规则。

---

## 10. RL 训练模块 (含 v3.0 MSSA 接口)

> **v2.17**：SB3 PPO/SAC 为主路径。**v2.15**：动作空间为 2 维同质 tanh `[p_ref, k_droop]`，SB3 `MlpPolicy` 直接支持。

### 10.1 启动流程

```
train.py 启动
├── 1. argparse 参数解析 (含 --config/--mic/--mssa-result v3.0 新增)
├── 2. 依赖检测 (SB3 → _ppo_core, gymnasium → _gym_stub)
├── 3. 数据加载 + 合成 + MIC 特征筛选 (v3.0)
├── 4. LSTM 模型加载 (或 Oracle)
├── 5. 环境创建 (MupcEnv, use_grid2op=True/False)
├── 6. 算法实例化 (SB3 PPO/SAC 或 NumPyPPO)
├── 7. 训练循环 (含 EvalCallback)
├── 8. 模型保存 + ONNX 导出 (含 metadata_props)
└── 9. stdout: PV_MAPE=0.xxx LOAD_MAPE=0.xxx (v3.0 MSSA 接口)
```

### 10.2 网络结构

```python
POLICY_KWARGS = {
    "net_arch": {"pi": [128, 128], "vf": [128, 128]},
    "activation_fn": nn.ReLU,
}
# 输出: Linear(128, 2) → tanh (v2.15 同质 2 维)
```

### 10.3 PPO 超参数

```python
PPO_CONFIG = {
    "learning_rate": 3e-4, "n_steps": 2048, "batch_size": 64,
    "n_epochs": 10, "gamma": 0.99, "gae_lambda": 0.95,
    "clip_range": 0.2, "ent_coef": 0.01, "vf_coef": 0.5,
    "max_grad_norm": 0.5,
}
```

### 10.4 MSSA 接口 (v3.0)

**`--config` JSON 参数**：

```python
def parse_mssa_config(config_path: str) -> dict:
    """解析 MSSA 生成的临时训练配置文件 (JSON)."""
    with open(config_path) as f:
        cfg = json.load(f)
    REQUIRED = ["hidden_size", "num_layers", "lr", "batch_size"]
    for k in REQUIRED:
        if k not in cfg:
            sys.stderr.write(f"[FATAL] Missing: {k}\n"); sys.exit(1)
    return cfg
```

**MSSA 键名 → 训练配置键名映射 (10 个超参)**：

```python
KEY_MAP = {
    "hidden_size": "hidden_dim",  # MSSA 名 → 本地名
    "lr": "learning_rate",
    "attn_score": "attn_score",
    "vmd_k": "vmd_k", "vmd_alpha": "vmd_alpha",
    "optimizer": "optimizer",
}
KNOWN_KEYS = {"hidden_dim", "num_layers", "learning_rate", "batch_size",
              "dropout", "input_window", "output_mode", "with_attention",
              "attn_score", "vmd_k", "vmd_alpha", "optimizer"}
# 未在 KEY_MAP 或 KNOWN_KEYS 中的 key → WARN 日志
```

**stdout MAPE 输出**：

```python
# 训练完成后
pv_mape = compute_mape(pv_pred, pv_true, step=1)
load_mape = compute_mape(load_pred, load_true, step=1)
print(f"PV_MAPE={pv_mape:.4f} LOAD_MAPE={load_mape:.4f}")
# MSSA 通过正则解析: PV_MAPE=([\d.]+)\s+LOAD_MAPE=([\d.]+)
```

**错误处理 (保证 MSSA 非零退出码)**：
```python
try:
    # 训练逻辑
    ...
    print(f"PV_MAPE={pv_mape:.4f} LOAD_MAPE={load_mape:.4f}")
except Exception as e:
    sys.stderr.write(f"[FATAL] {e}\n")
    sys.exit(1)  # 非零退出码, MSSA 标记为无效
```

**MSSA 调用流程**：

```
MSSA 超参搜索 (通过 train.py --config JSON 接口驱动, 工具端位于 tools/mssa_optimizer/)
  │  每轮生成 JSON: {"hidden_size": 96, ...}
  ├──→ subprocess: python train.py --config /tmp/mssa_iter_42.json
  │      stdout: PV_MAPE=0.073 LOAD_MAPE=0.110
  ├──→ ResultParser: 正则提取 → weighted_MAPE = 0.5×PV + 0.5×LOAD
  │      退出码非零 → penalty_score = 1e6
  └──→ 下一轮迭代, 直到收敛
```

### 10.5 多模式训练

```python
class MultiModeWrapper(gym.Wrapper):
    MODES = ["MODE-01","MODE-02","MODE-03","MODE-04","MODE-05"]
    def reset(self, **kwargs):
        if self.env.unwrapped.mode == "all":
            chosen = np.random.choice(self.MODES)
            self.env.unwrapped.set_mode(chosen)
        return self.env.reset(**kwargs)
```

---

## 11. 纯 NumPy PPO 后备

SB3/Gymnasium/Torch 任一不可用时自动降级。

```
Input(78 or 79) → Linear(128) → ReLU → Linear(128) → ReLU
                    ├── actor:  Linear(2) → Tanh (v2.15)
                    └── critic: Linear(1)
```

```python
class NumPyPPO:
    """纯 NumPy PPO: Clipped Objective + GAE + MomentumSGD."""
    def learn(self, total_timesteps, callback=None): ...
    def save_weights(self, path: str): ...
    def load_weights(self, path: str): ...
```

### 11.1 GAE 计算

```python
def compute_gae(rewards, values, dones, gamma=0.99, lam=0.95):
    advantages = np.zeros_like(rewards, dtype=np.float32)
    last_adv = 0.0
    for t in reversed(range(len(rewards))):
        if dones[t]:
            delta = rewards[t] - values[t]
            last_adv = 0.0
        else:
            delta = rewards[t] + gamma * values[t+1] - values[t]
        advantages[t] = last_adv = delta + gamma * lam * last_adv
    returns = advantages + values[:-1]
    return advantages, returns
```

---

## 12. Gymnasium 桩模块

```python
try:
    import gymnasium as gym
    _GYM_AVAILABLE = True
except ImportError:
    from _gym_stub import Env as _BaseEnv, Box
    _GYM_AVAILABLE = False
```

---

## 13. ONNX 模型导出 (v3.0 增强)

### 13.1 RL 策略导出

```python
class RLPolicyExport(nn.Module):
    """ONNX 导出壳 (v2.15: 2 维动作 [p_ref, k_droop])."""
    def __init__(self, obs_dim=78, act_dim=2, hidden=[128, 128]):
        self.shared = nn.Sequential(
            nn.Linear(obs_dim, hidden[0]), nn.ReLU(),
            nn.Linear(hidden[0], hidden[1]), nn.ReLU())
        self.actor = nn.Linear(hidden[1], act_dim)

    def forward(self, x):
        return torch.tanh(self.actor(self.shared(x)))
```

### 13.2 LSTM + Attention 导出

```python
def export_lstm_attn(checkpoint_path: str, output_dir: str = "./exported_models/",
                     input_window: int = 12, with_attention: bool = True,
                     bidirectional: bool = False,
                     metadata: dict | None = None) -> str:
    """导出 LSTM+Attention 模型 (v3.0).
    - with_attention=True: Attention 图嵌入 ONNX (AdditiveAttention 作为子模块)
    - bidirectional=True: 导出 BiLSTM 版本 (R2)
    """
```

**新增输出节点**（可选）：
```python
# 导出时额外输出 attention_weights 用于可视化分析
torch.onnx.export(
    wrapper, dummy_input, onnx_path,
    output_names=["forecast", "attention_weights"],  # 双输出
    ...
)
```

### 13.3 metadata_props 注入 (10 键)

```python
metadata = {
    "mupc_model_type": "lstm",          # "lstm" / "bilstm" / "error_correction"
    "mupc_with_attention": "true",      # "true" / "false"
    "mupc_with_vmd": "false",           # "true" / "false"
    "mupc_mic_topk": "7",               # MIC 筛选特征数
    "mupc_output_horizon": "15",
    "mupc_input_window": str(input_window),
    "mupc_hidden_size": str(hidden_dim),
    "mupc_num_layers": str(num_layers),
    "mupc_direction": "forward",        # "forward" / "bidirectional"
    "mupc_version": "v3.1",
}
```

**下游校验行为**：启动时交叉校验 metadata 与配置文件，不一致时拒绝加载或 WARN 回退。

### 13.4 导出模型文件清单

| 函数 | 导出文件 | R1/R2 | INT8 上限 |
|------|---------|-------|-----------|
| `export_lstm_attn()` | `lstm_attn.onnx` | R1 必选 | ≤ 8MB |
| `export_lstm_attn(bidirectional=True)` | `bilstm_attn.onnx` | R2 可选 | ≤ 12MB |
| `export_error_correction()` | `error_correction.onnx` | R2 可选 | ≤ 3MB |

三个模型独立 OTA 升级/降级，各自独立 `RknnRuntime` 实例。

### 13.5 新增 CLI 参数

```python
parser.add_argument("--input-window", type=int, default=12)
parser.add_argument("--with-attention", action="store_true", default=True)
parser.add_argument("--bidirectional", action="store_true")
parser.add_argument("--metadata", type=str, default=None)
```

---

## 14. 数据交换接口 (v3.0)

### 14.1 MIC JSON 读取

> **v3.1 已实现**: `tools/mic_analysis.py` 生成 MIC JSON，minepy 不可用时降级 Pearson。

```python
def load_mic_features(json_path: str) -> tuple[list[str], int]:
    """读取 MIC 分析结果, 返回 (selected_features, top_k).
    若 len(selected) != top_k → WARN 日志.
    """
    with open(json_path) as f:
        mic = json.load(f)
    selected = [feat["name"] for feat in mic["features"] if feat["selected"]]
    top_k = mic["top_k"]
    if len(selected) != top_k:
        sys.stderr.write(f"[WARN] MIC selected {len(selected)} != top_k {top_k}\n")
    return selected, top_k
```

### 14.2 MSSA JSON 读取

```python
def load_mssa_result(json_path: str) -> dict:
    """读取 MSSA 搜索结果, 返回最优超参.
    quality_flag="unusable" → WARN + 回退人工基线.
    """
    with open(json_path) as f:
        mssa = json.load(f)
    if mssa.get("quality_flag") == "unusable":
        sys.stderr.write("[WARN] MSSA unusable, using manual baseline\n")
    return mssa["best_hyperparameters"]
```

### 14.3 训练数据指纹

```python
def compute_data_fingerprint(data_dir: str) -> str:
    """CSV 文件 SHA256 前 16 位, 用于 MSSA 缓存命中判断."""
    import hashlib
    hasher = hashlib.sha256()
    for csv_path in sorted(Path(data_dir).rglob("*.csv")):
        hasher.update(csv_path.read_bytes())
    return hasher.hexdigest()[:16]
```

---

## 15. 配置与常量

### 15.1 物理常量 (v3.1)

```python
TRANSFORMER_KVA = 200.0; BATTERY_CAPACITY_KWH = 100.0
P_BATT_MAX_KW = 50.0; Q_BATT_MAX_KVAR = 300.0
PV_ARRAY_KW = 150.0; LOAD_PEAK_KW = 60.0
LOAD_SHED_MAX_KW = 60.0; LOAD_PF = 0.90
SOC_MIN = 0.10; SOC_MAX = 0.90
OVERLOAD_THRESHOLD = 0.85; DT_HOURS = 0.25
CONTRACT_DEMAND_KW = 200.0; GRID_EMISSION_FACTOR = 0.581

# 下垂控制
K_DROOP_MIN = 0.0; K_DROOP_MAX = 30.0
K_DROOP_RAMP_LIMIT = 10.0   # ACT-02
P_REF_RAMP_LIMIT_KW = 50.0  # ACT-01

# D10 冲击预备度
SHOCK_THRESHOLD_KW = 10.0; SOC_RESERVE_TARGET = 0.7
P_REF_RESERVE_TARGET = 10.0; SHOCK_READINESS_W_SOC = 20.0
SHOCK_READINESS_W_P = 10.0
```

### 15.2 YAML 配置文件系统

配置文件 `config/mupc_env_config.yaml` 统一管理所有环境参数，支持 `--config` 参数。

配置加载器 `config/config_manager.py`：`MupcConfig.from_yaml(path)` / `load_config(path)` / `get_config()`

### 15.3 训练与仿真配置

```python
# Grid2Op 电压仿真
GRID2OP_CONFIG = {
    "use_grid2op": True,              # 默认使用 Grid2Op
    "backend": "lightsim",            # lightsim2grid > pandapower
    "storage_soc_init": 0.5,           # 初始 SOC
}

# 潮流计算
POWER_FLOW_CONFIG = {
    "calculate_voltage_angles": False,
    "trafo_model": "t",               # 变压器模型
    "lineload_tolerance": 1e-4,
}

# 训练
TRAIN_DEFAULTS = {
    "total_timesteps": 175200,
    "seed": 42,
    "checkpoint_path": "./checkpoints/",
}
EPISODE_LENGTH = 96  # 1 天 = 96 步 × 15 分钟
```

---

## 16. 错误处理

| 层级 | 场景 | 处理 |
|------|------|------|
| FATAL | 数据目录不存在 | sys.exit(1) |
| FATAL | LSTM 未提供 + Oracle 不可用 | sys.exit(1) |
| FATAL | MSSA 必需配置键缺失 | sys.exit(1) |
| ERROR | checkpoint 文件损坏 | ERROR 日志, 退出 |
| WARN | SB3 不可用 | 自动切换 _ppo_core.py |
| WARN | Gymnasium 不可用 | 自动切换 _gym_stub.py |
| WARN | Grid2Op 不可用 | 降级到 VoltageSimulator |
| WARN | lightsim2grid 不可用 | 降级到 PandaPowerBackend |
| WARN | 潮流计算不收敛 | 回退到上一时刻安全电压 |
| WARN | ONNX runtime 不可用 | 跳过推理验证 |
| WARN | MIC 选中数 ≠ top_k | WARN 日志 |
| WARN | MSSA quality_flag=unusable | 回退人工基线 |
| WARN | MSSA key 未识别 | WARN 日志, 跳过 |
| INFO | GPU 不可用 | 自动回退 CPU |

---

## 17. 技术决策记录

### TD-01：训练环境合成缺失字段
合成部署端由实时数据源提供的字段（电压/需量/调度），因 SMART-DS 仅含光伏/负荷/气象。

### TD-02：观测维度变更
v2.5 58/59 → v2.12 58/59 → v2.14 61/62 → v2.16 63/64 → v2.17 78/79 → v3.0 78/79 (LSTM 内部升级为 p10p50p90) → v3.1 78/79 (不变, 内部升级 TCN + QuantileLoss)。

### TD-03：LSTM 训练在管线内
SMART-DS 数据可用于训练 LSTM，管线需要预测值作为 RL 状态输入。v3.1: 新增 Data Quality 标签过滤 (quality>=1 样本不参与训练)。

### TD-04：训练步长保持 15 分钟
15 分钟粒度下 RL 已可学到有效策略。动作约束规则按 Δ/步 理解。

### TD-05：电压仿真技术选型
Grid2Op + Pandapower 三相潮流为主，VoltageSimulator 为降级方案。

### TD-06：分层控制架构
Q 控制交由实时控制核心根据电压闭环调节，RL 仅输出 P 控制指令。

### TD-07：AdditiveAttention 选型 (v3.0)
选择加法注意力而非自注意力：24 步短序列足够、参数量小 (H² vs 3H²)、全算子 ONNX 标准 NPU 直接推理。

### TD-08：三个模型独立部署 (v3.0)
lstm_attn (主力) + bilstm_attn (精英替补, Go/No-Go) + error_correction (校对员, Bias Gate)。独立 RknnRuntime、独立 OTA、独立降级。

### TD-09：MSSA 离线超参优化 (v3.0)
麻雀搜索算法替代人工 grid search。离线 Python 工具，不进入 RK3588 部署路径。

### TD-10：电压死区 + 功率变化率惩罚
MODE-01 奖励函数新增 ±5% 电压死区（越限连续 2 步触发）和 R_ramp 功率变化率惩罚（归一化到 C-rate），防止 AI 对瞬时高频脉冲过度响应。v3.1: 下垂公式修正为 P_output = P_ref - k_droop × ΔV, ACT-02=10 kW/V/步, w7=0.5。

### TD-11：Grid2Op 组合模式 (v2.3)
MupcEnv 内部持有 Grid2Op 实例（组合模式），而非继承 Grid2Op 环境基类。Grid2Op 与 Gymnasium 基类不兼容，组合模式使 Grid2Op 完全内部化。

### TD-12：SOC 双向同步 (v2.3)
MupcEnv._soc 与 Grid2Op storage.soc 每步双向同步，保证两者一致。潮流不收敛时 SOC 同步照常进行。

### TD-13：潮流不收敛回退策略 (v2.3)
潮流计算不收敛或电压为 NaN 时，回退到上一时刻安全电压，`has_illegal=True` 标记但不触发 terminated。

### TD-14：TCN 时域卷积特征提取 (v3.1)
4 层因果膨胀卷积 (dilation=[1,2,4,8], kernel=3, 64 filters)，插入在 VMD 与 LSTM 之间。捕获多尺度时序模式 (15 步感受野)，~39K 参数 (≤100K 目标)，默认开启。全 ONNX 标准算子 (Conv1D/BN/ReLU/Add)，RK3588 NPU 原生支持，INT8 ≤0.5MB，延迟开销 ≤20ms。

### TD-15：QuantileLoss 真分位数回归 (v3.1)
Pinball Loss 替换 MSE/Huber 成为默认损失函数，tau=[0.10, 0.50, 0.90]。输出 P10/P50/P90 真分位数预测 (非点估计)，下游可做 P90 覆盖率验证 (实际值 ≤ P90 的比例应 ≥ 88%)，支持风险管理决策。

### TD-16：安全边界 Log-barrier 软化 (v3.1 专家评审)
将安全覆盖的硬惩罚 (0→负值跳变) 补充为 Log-barrier 连续梯度。对电压上下限 (1.15/0.85)、变压器负载 (0.85)、SOC 上下限 (0.10/0.90) 共 4 个边界施加 -log(margin) 惩罚。远离边界 ≈ 0 不干扰训练，接近边界时梯度递增引导远离。以 w=0.1 小权重加入总奖励。

### TD-17：Welford 逐分量 EMA 自适应归一化 (v3.1 专家评审)
扩展 Welford 从总分归一化到 6 个关键奖励分量的逐分量 EMA 跟踪 (alpha=0.001)。为检测固定归一化除数的分布漂移提供数据基础，统计量暴露在 get_welford_stats() 供监控。

---

## 修订历史

### v3.1 (2026-06-21)

| 序号 | 修订项 | 说明 |
|------|--------|------|
| 1 | 合并 v3.0 独立设计文档 | 原 `2026-06-21-MUPC-RL训练管线-v3.0-设计文档.md` 全部 8 章融入 |
| 2 | ACT-02 变化率: 30→10 kW/V/步 | 对齐下游 PRD §6.5 |
| 3 | w7: 0.3→0.5 | 对齐下游 PRD §7.2 |
| 4 | CONTRACT_DEMAND_KW: 300→200 | 对齐下游 data_fusion.rs |
| 5 | 下垂公式: + → - | `P_ref - k_droop × ΔV` |
| 6 | ActionValidator: 3维→2维 ACT-01~05 | S-circle, v3.1 |
| 7 | VMD tol: 1e-7→1e-6 | 对齐下游 PRD §3.3.2 |

### v2.17 (SB3 升级主路径)

| 序号 | 修订项 | 说明 |
|------|--------|------|
| 1 | SB3 PPO/SAC 升级为主路径 | v2.15 2维同质 tanh, MlpPolicy 直接支持 |
| 2 | 新增 `--algo-backend` 参数 | auto(默认)/sb3/numpy |
| 3 | 依赖检测升级 | _resolve_backend() 检查 SB3/Gymnasium/Torch |
| 4 | _ppo_core.py act_dim 修复 | 5→2, 与 v2.15 一致 |

### v2.15 (动作空间 5D→2D)

| 序号 | 修订项 | 说明 |
|------|--------|------|
| 1 | 动作空间 5D→2D | [p_ref, k_droop] 全 tanh |
| 2 | load_shedding/pv_limit 下沉 | strategy-engine 本地策略 |
| 3 | confidence → ModelOutput 元数据 | 不在 RL 动作空间 |
| 4 | Welford 归一化启用 | 跨 episode 累积 mean/variance |
| 5 | Q_batt 实时控制闭环 | 不经过 RL |

### v2.11 (SCENE-01 奖励重构)

| 序号 | 修订项 |
|------|--------|
| 1 | P-Q 协同度奖励 (Sigmoid 平滑化) |
| 2 | 差异化弃光奖励 (v_avg≥1.05 方向检查) |
| 3 | 下垂系数平滑惩罚 R_smooth (w7) |

### v2.10 (双参数下垂控制)

| 序号 | 修订项 |
|------|--------|
| 1 | 新增下垂模式 (4维动作空间 [P_ref, k_droop, load_shedding, pv_limit]) |
| 2 | ACT-DUAL-01~05 约束规则 |
| 3 | NumPyPPO dual_mode 支持 |

### v2.6 (物理参数全面修正)

| 序号 | 修订项 |
|------|--------|
| 1 | 动作空间 2维→3维 (恢复 pv_limit) |
| 2 | 物理参数修正: P_BATT 500→50kW, BATTERY 200→100kWh, TRAFO 500→200kVA |
| 3 | ACT-03 S_MAX 500→200kVA |
| 4 | 新增 YAML 配置文件系统 |

### v2.5 (状态空间扩展)

| 序号 | 修订项 |
|------|--------|
| 1 | 状态空间 56/57→58/59 维 (新增 D5 气象) |
| 2 | 观测空间索引修正 |

### v2.4 (分层控制架构)

| 序号 | 修订项 |
|------|--------|
| 1 | 动作空间 4维→2维 (Q 控制交实时核心) |
| 2 | SCENE-01 新增电压死区 + R_ramp + w4/w5 |

### v2.3 (Grid2Op 电压仿真)

| 序号 | 修订项 |
|------|--------|
| 1 | 集成 Grid2Op + Pandapower 三相潮流 |
| 2 | VoltageSimulator 降级方案 |
| 3 | SOC 双向同步机制 |
| 4 | 新增 mupc_env/grid2op/ 模块 |

---

**文档状态**：[DESIGN_APPROVED] — 2026-06-21
**关联 PRD**：`docs/superpowers/specs/2026-06-06-MUPC-RL训练管线-PRD.md` v3.0
**原独立 v3.0 设计文档**：已融入本文档，不再需要单独维护

---

## 11. MSSA 超参优化工具设计

> **v3.1 迁移** | 原位于 MUPC AI 引擎设计文档 §15，现迁移至训练管线设计文档。
> MSSA 是离线训练阶段工具（Python），非 RK3588 运行时（Rust），归于训练管线更合理。


> **v3.0 合并新增** | **来源：** `docs/superpowers/plans/2026-06-21-预测增强分层混合架构-DESIGN.md` v3.0, Section 12
> **PRD 对应：** Section 3.5 (F5: 超参自动优化) + Section 7.4.2 (JSON Schema)
> **与 Rust 的关系：** 零。搜索结果通过 JSON 被训练管线消费，最终体现为 ONNX 模型维度变化。Rust 推理端不感知搜索过程。

### 11.1 模块定位

| 维度 | 说明 |
|------|------|
| **模块名称** | `tools/mssa_optimizer/` |
| **语言** | Python 3.9+，纯 Python（无 C 扩展） |
| **依赖** | numpy >= 1.24、scipy >= 1.10、pyyaml >= 6.0（详见 Python 依赖表） |
| **运行环境** | MUPC-AI2 训练服务器（Linux x86_64），不在 RK3588 上运行 |
| **触发方式** | 命令行 `python -m tools.mssa_optimizer --config mssa_search_config.yaml` |
| **对 Rust 的影响** | 零。搜索结果通过 JSON 被训练管线消费，最终体现为 ONNX 模型维度变化。Rust 推理端不感知搜索过程 |
| **PRD 对应** | Section 3.5 (F5: 超参自动优化) + Section 7.4.2 (JSON Schema) |

**设计原则：**
- **无状态**：每次运行独立，不依赖 DB 或外部服务（缓存文件除外）
- **幂等**：相同搜索配置 + 相同训练数据 → 确定性结果（固定 random seed）
- **可中断**：支持 Ctrl+C 优雅退出，输出当前最优解（通过 signal handler 捕获 SIGINT）
- **可观测**：每次迭代输出 `[INFO]` 级别日志（当前迭代数、最优 MAPE、种群多样性指标）

**备选（IPSO 降级路径）：** IPSO（Improved Particle Swarm Optimization，改进粒子群优化）作为备选方案。当 MSSA 搜索时间超预期（单次搜索 > 2 小时）或收敛不稳定（连续 3 次重启后最优解 MAPE > 人工基线 MAPE * 1.1）时，通过配置文件 `algorithm: "IPSO"` 切换。IPSO 实现简单、收敛快（通常 <= 30 次迭代），但全局搜索能力弱于 MSSA。切换时输出 JSON 中 `search_metadata.algorithm` 字段自动变更为 `"IPSO"`，其余 JSON Schema 保持一致。IPSO 降级时 algorithm 字段值为 `"IPSO"`（与 PRD v1.1 Schema 的 const 约束存在已知偏差，将在 PRD 下一版本修正为 enum）。

**Python 依赖说明：**

| 依赖 | 版本 | 用途 |
|------|------|------|
| `numpy` | >= 1.24 | 种群矩阵运算（向量化操作）、伪随机数生成（PCG64 或 MT19937）、数值稳定性保护 |
| `scipy` | >= 1.10 | `scipy.stats` 统计检验（Mann-Whitney U）、`scipy.spatial.distance` 种群多样性度量、`scipy.optimize` 辅助（仅调试用） |
| `pyyaml` | >= 6.0 | 搜索配置文件解析 |
| `json` | 标准库 | JSON 输出（对齐 PRD Section 7.4.2 JSON Schema） |
| `hashlib` | 标准库 | 超参组合 SHA256 指纹（缓存键生成） |
| `subprocess` | 标准库 | 调用训练脚本 |
| `time` | 标准库 | 超时控制 |
| `pathlib` | 标准库 | 临时配置路径管理、缓存文件路径 |
| `tempfile` | 标准库 | 临时训练配置文件创建（原子写入 + 自动清理） |

**与 MUPC-AI2 训练管线的集成点：**

```
tools/mssa_optimizer/                         MUPC-AI2 训练管线
+----------------------------------+         +-----------------------------------+
| mssa.py         (算法核心)        |         |                                   |
| search_space.py (搜索空间定义)     |  JSON   |  读取 best_hyperparameters       |
| objective.py    (目标函数) ──subprocess──→ |  train.py --config <tmp_config>  |
|                                   |  (调用)  |  export_onnx.py                  |
| output.py       (JSON 输出)       |         |                                   |
| config.py       (配置加载/校验)    |         |                                   |
| test_mssa.py    (单元测试)        |         |                                   |
+----------------------------------+         +-----------------------------------+

数据流：
1. mssa.py 生成一组超参 → objective.py
2. objective.py 写临时训练配置 YAML → 调用 subprocess 运行 train.py
3. train.py 训练 → 输出验证集 MAPE → objective.py 解析 stdout/结果文件
4. objective.py 返回加权目标函数值 → mssa.py 用于种群更新
5. 搜索结束 → output.py 写 mssa_search_result.json → MUPC-AI2 训练管线消费
```

### 11.2 文件结构

```
tools/mssa_optimizer/
├── __init__.py                # 包初始化，导出公共 API (~5 行)
├── mssa.py                   # MSSA 算法核心 (预计 ~300 行)
│   ├── class MssaOptimizer   #   主控制器：种群初始化、迭代循环、终止判定
│   ├── class Population      #   种群管理：位置矩阵 (N, D)、适应度向量 (N,)
│   ├── class Discoverer      #   发现者更新逻辑
│   ├── class Joiner          #   加入者更新逻辑
│   ├── class Scout           #   侦察者更新逻辑
│   ├── good_point_set_init() #   佳点集初始化函数
│   ├── opposition_learning() #   反向学习增强函数
│   └── corsi_mutation()      #   Corsi 变异扰动函数
├── search_space.py            # 搜索空间定义与编码/解码 (~150 行)
│   ├── class SearchSpace      #   10 维超参搜索空间定义
│   ├── class HyperParam      #   单个超参定义（名称/类型/范围/步长）
│   ├── encode() / decode()    #   混合编码/解码（离散→整数索引，连续→浮点，枚举→one-hot）
│   └── random_sample()        #   搜索空间内均匀随机采样
├── objective.py               # 目标函数 (~200 行)
│   ├── class ObjectiveFunc    #   目标函数包装
│   ├── class TrainingRunner  #   子进程调用封装（train.py）
│   ├── class CacheManager    #   评估缓存管理（mssa_cache.json）
│   └── class ResultParser    #   训练输出解析（提取 MAPE_pv、MAPE_load）
├── config.py                  # 配置加载/校验 (~100 行)
│   ├── class MssaConfig       #   搜索配置结构体（种群参数、终止条件、算法选择）
│   ├── load_config()          #   YAML 加载 + schema 校验
│   └── validate()             #   参数合法性校验（范围检查、互斥检查）
├── output.py                  # JSON 输出 (~80 行)
│   ├── class SearchOutput     #   搜索输出构建器
│   ├── to_json()              #   序列化为 PRD 7.4.2 兼容 JSON
│   └── validate_output()      #   JSON Schema 自校验
├── mssa_cache.json            # 评估缓存（运行时自动生成，.gitignore 排除）
└── test_mssa.py               # 单元测试 (~200 行)
    ├── test_good_point_set()  #   佳点集初始化均匀性测试
    ├── test_encode_decode()   #   编解码往返一致性测试
    ├── test_objective_cache() #   缓存命中/未命中测试
    ├── test_convergence()     #   收敛条件触发测试（模拟目标函数）
    ├── test_output_schema()   #   输出 JSON 对 PRD 7.4.2 schema 合规性
    └── test_ipso_fallback()   #   IPSO 降级路径测试
```

**文件总行数预估：**

| 文件 | 预估行数 | 说明 |
|------|----------|------|
| `__init__.py` | ~5 | 包导出 |
| `mssa.py` | ~300 | 算法核心（种群 + 三群体 + 增强策略） |
| `search_space.py` | ~150 | 10 维搜索空间 + 混合编解码 |
| `objective.py` | ~200 | 目标函数 + 训练子进程 + 缓存 |
| `config.py` | ~100 | YAML 配置加载 + 校验 |
| `output.py` | ~80 | JSON 输出构建 + schema 自校验 |
| `test_mssa.py` | ~200 | 6 项单元测试 |
| `mssa_search_config.yaml` | ~60 | 配置模板（含注释） |
| **合计** | **~1095** | |

### 11.3 MSSA 算法设计

#### 15.3.1 算法总览

MSSA 模拟麻雀群体觅食行为，将超参搜索问题映射为群体在 D 维搜索空间中的位置优化：

```
算法流程（伪代码）：

Input:  搜索空间 Ω (D=10 维), 种群大小 N (默认 30),
        发现者比例 p_d=0.2, 侦察者比例 p_s=0.1,
        最大迭代 T_max (默认 50), 收敛阈值 ε=1e-4,
        停滞容忍 S_max (默认 10), 总时间上限 T_wall (默认 7200s)

Output: 最优超参 x_best, 最优目标值 f_best, 收敛曲线, 搜索轨迹

1. 初始化:
   1a. 佳点集生成 N 个初始个体位置 X = {x_1, ..., x_N}  (15.3.3)
   1b. 评估所有个体 f_i = ObjectiveFunc(x_i)
   1c. 排序: 按 f_i 升序排列 (最小化问题)
   1d. 记录全局最优: x_best = x_1, f_best = f_1
   1e. 初始化停滞计数: stagnation = 0, prev_best = f_best
   1f. 初始化缓存: CacheManager.load()

2. 主循环 (for iter = 1 to T_max):
   2a. 群体角色分配:
       - 前 p_d*N 个体 = 发现者 (Discoverer)
       - 中间 (1-p_d-p_s)*N 个体 = 加入者 (Joiner)
       - 后 p_s*N 个体 = 侦察者 (Scout)

   2b. 发现者更新 (15.3.4)
   2c. 加入者更新 (15.3.5)
   2d. 侦察者更新 (15.3.6)
   2e. 边界处理: 裁剪到搜索空间 Ω
   2f. 评估所有新个体 f_i_new = ObjectiveFunc(x_i_new)  (带缓存)
   2g. 精英保留: 若 min(f_new) > f_best, 保留上一代最优个体
   2h. 更新全局最优

   2i. 反向学习增强 (15.3.7): 每 5 次迭代执行一次
   2j. Corsi 变异扰动 (15.3.8): 若 stagnation >= S_max, 对停滞个体施加强变异

   2k. 收敛判定 (15.6):
       - 若 abs(f_best - prev_best) < ε: stagnation += 1
       - 否则: stagnation = 0
       - 若 stagnation >= S_max: 终止 (收敛)
       - 若 elapsed > T_wall: 终止 (超时)

   2l. 保存收敛曲线: convergence_curve[iter] = f_best
       保存参数轨迹: trajectory[param][iter] = x_best[param]

   2m. 检查 Ctrl+C 中断信号 → 优雅退出

3. 输出:
   3a. 构建 SearchOutput (符合 PRD 7.4.2 JSON Schema)
   3b. 写 mssa_search_result.json
   3c. 持久化缓存 (mssa_cache.json)
```

#### 15.3.2 种群规模与角色分配

| 参数 | 符号 | 默认值 | 可配置范围 | 说明 |
|------|------|--------|------------|------|
| 种群大小 | N | 30 | [10, 100] | 种群个体总数。N 越大搜索越充分，但目标函数评估次数 = N * T，训练成本高。若搜索空间较大或训练成本允许，可适当增大 N 至 50~60 以提升全局搜索能力 |
| 发现者比例 | p_d | 0.20 | [0.10, 0.30] | 全局勘探主力，数量少以保证快速收敛 |
| 侦察者比例 | p_s | 0.10 | [0.05, 0.20] | 预警个体，感知危险后引导群体逃离局部最优 |
| 加入者比例 | p_j = 1-p_d-p_s | 0.70 | -- | 跟随发现者在局部精细搜索 |

**角色分配规则：** 每代按适应度排序后：
- 前 `N_d = ceil(p_d * N)` 个体 = 发现者（适应度最优，负责全局勘探）
- 后 `N_s = ceil(p_s * N)` 个体 = 侦察者（适应度最差，负责预警和全局逃离）
- 中间 `N_j = N - N_d - N_s` 个体 = 加入者（中等适应度，负责局部开发）

默认配置：N=30, p_d=0.20, p_s=0.10 → N_d=6, N_s=3, N_j=21。

#### 15.3.3 佳点集初始化

传统随机初始化可能导致个体在搜索空间中聚集，降低种群多样性。佳点集（Good Point Set）方法生成均匀分布的初始种群：

```
x_{i,j} = (i * golden_ratio^j) mod 1 * (ub_j - lb_j) + lb_j

其中:
  i ∈ [1, N]:      个体索引
  j ∈ [1, D]:      维度索引 (D=10)
  golden_ratio:    黄金分割比 φ = (sqrt(5) - 1) / 2 ≈ 0.6180339887
  ub_j, lb_j:      第 j 维搜索上下界
```

**为什么用黄金分割比：** φ 是无理数，{i * φ^j mod 1} 序列在 [0,1] 上具有低偏差（low discrepancy），比伪随机序列更均匀。

**验证标准：** 单元测试验证佳点集生成的 N 个个体在 10 维空间中的成对最小欧氏距离 >= 随机初始化的 1.2 倍（种群多样性指标）。

#### 15.3.4 发现者位置更新

发现者负责在全局范围内搜索更优区域。位置更新公式：

```
x_{i,j}^{t+1} = x_{i,j}^t * exp(-i / (alpha * T_max))   若 R_2 < ST  (安全：广泛探索)
x_{i,j}^{t+1} = x_{i,j}^t + Q * L                        若 R_2 >= ST (危险：飞离当前位置)

其中:
  t:          当前迭代
  alpha:      (0, 1] 均匀随机数
  R_2:        [0, 1] 预警值 (每代随机生成)
  ST:         安全阈值 (默认 0.8)
  Q:          标准正态分布随机数 N(0, 1)
  L:          1×D 全 1 向量
```

#### 15.3.5 加入者位置更新

加入者跟随发现者，在发现者占领的中优区域进行局部搜索：

```
x_{i,j}^{t+1} = Q * exp((x_worst_j^t - x_{i,j}^t) / i^2)    若 i > N/2  (低排名加入者：飞向最优发现者)
x_{i,j}^{t+1} = x_best_j^t + |x_{i,j}^t - x_best_j^t| * A^+ * L   若 i <= N/2 (高排名加入者：在最优附近局部搜索)

其中:
  x_worst_j^t:   第 t 代全局最差个体的第 j 维
  x_best_j^t:    第 t 代全局最优个体的第 j 维
  A^+:           随机 1×D 向量，每个元素 = 1 或 -1 各 50% 概率
  A^+ = A^T * (A * A^T)^{-1}
```

#### 15.3.6 侦察者位置更新

侦察者感知危险，引导种群逃离局部最优：

```
x_{i,j}^{t+1} = x_best_j^t + beta * |x_{i,j}^t - x_best_j^t|    若 f_i > f_best  (当前个体比最优差)
x_{i,j}^{t+1} = x_{i,j}^t + K * (|x_{i,j}^t - x_worst_j^t| / (|f_i - f_worst| + 1e-8))    若 f_i == f_best  (当前个体在最优位置)

其中:
  beta:   标准正态分布随机数 N(0, 1)，控制步长
  K:      [-1, 1] 均匀随机数，控制方向
  f_i:    第 i 个体的适应度值
```

#### 15.3.7 反向学习增强

每 5 次迭代对全体个体执行一次反向学习（Opposition-Based Learning），生成每个个体的"反向解"并保留更优者：

```
x_opposition_{i,j} = lb_j + ub_j - x_{i,j}

若 f(x_opposition_i) < f(x_i):
    替换 x_i = x_opposition_i
否则:
    保留 x_i
```

#### 15.3.8 Corsi 变异扰动

当连续 S_max 次迭代（默认 10 次）全局最优无改善时，Corsi 变异针对收敛停滞的个体施加自适应强度变异：

```
停滞检测:
  若 stagnation_count >= S_max (默认 10):
    对适应度最差的后 50% 个体施加 Corsi 变异

Corsi 变异公式:
  x_{i,j}^{new} = x_{i,j} + Corsi * (ub_j - lb_j) * randn()

  Corsi 系数:
    Corsi = C_0 * exp(-beta_corsi * stagnation / S_max) * diversity

  diversity = mean(pairwise_euclidean(所有个体)) / max_pairwise_euclidean(初始种群)

其中:
  C_0:            初始变异强度 (默认 0.1)
  beta_corsi:     衰减因子 (默认 2.0)
  stagnation:     当前停滞次数
  diversity:      当前种群多样性分数 (0~1)，多样性越低变异越强
```

**自适应特性：**
- 停滞时间越长，Corsi 系数越小（变异幅度衰减，防止永远不收敛）
- 种群多样性越低，Corsi 系数越大（强变异打破同质化）
- 变异仅作用于后 50% 个体（保留精英个体不受扰动）

**恢复机制：** Corsi 变异后，若全局最优在 3 次迭代内更新，重置 `stagnation = 0`；否则继续增加 stagnation 计数。

### 11.4 目标函数设计

#### 15.4.1 目标函数定义

```
f(x) = weighted_mape = 0.5 * MAPE_pv + 0.5 * MAPE_load  (最小化)

其中:
  x = [hidden_size, num_layers, attn_score, vmd_k, vmd_alpha,
       lr, batch_size, dropout, optimizer, input_window]  (10 维向量)

  MAPE_pv  = 训练脚本输出: 光伏测试集 MAPE (第 1 步)
  MAPE_load = 训练脚本输出: 负荷测试集 MAPE (第 1 步)
```

**权重说明：** 光伏和负荷在当前系统中等权重（0.5 : 0.5）。`MssaConfig` 中提供 `objective_weights: [pv_weight, load_weight]` 配置项，允许后续调整为非等权。

#### 15.4.2 目标函数执行流程

```
ObjectiveFunc::evaluate(x) → float:

Step 1: 超参解码 (CPU, < 1ms)
  decode(x) → hyperparams_dict
    - hidden_size: int(floor(x[0] + 0.5)) → 取最近的离散值 (32, 64, 96, 128)
    - num_layers: int(floor(x[1] + 0.5)) → 取最近的离散值 (1, 2, 3)
    - attn_score: argmax(x[2:5]) → {"additive", "dot", "general"}
    - vmd_k: int(clip(floor(x[5] + 0.5), 2, 10))
    - vmd_alpha: float(clip(x[6], 100, 5000))
    - lr: 10^clip(x[7], -4, -2)  (log 空间编码)
    - batch_size: int(floor(x[8] + 0.5)) → 取最近的离散值 (16, 32, 64, 128)
    - dropout: float(clip(x[9], 0.0, 0.5))
    - optimizer: argmax(x[10:13]) → {"Adam", "AdamW", "RMSprop"}
    - input_window: int(floor(x[13] + 0.5)) → 取最近的离散值 (12, 24, 36)
  注: 编码维度 D = 2(离散) + 3(one-hot枚举) + 2(连续/整数) + 3(one-hot枚举) + 3(离散) = 13 维
      但逻辑搜索空间维度 = 10 (见 15.5 详细映射)
  注: 使用 floor(x + 0.5) 代替 round()，确保 .5 边界始终向上舍入，消除银行家舍入的非确定性

Step 2: 缓存查找 (CPU, < 1ms)
  fingerprint = sha256(json.dumps(hyperparams_dict, sort_keys=True))[:16]
  若 fingerprint ∈ cache:
    return cache[fingerprint]  ← 跳过训练

Step 3: 写临时训练配置 (CPU, < 10ms)
  temp_config = tempfile.NamedTemporaryFile(suffix=".yaml", delete=False)
  将 hyperparams_dict 注入训练配置模板 → 写 temp_config

Step 4: 调用训练脚本 (外部进程, 变长)
  cmd = [sys.executable, "train.py", "--config", temp_config.name]
  proc = subprocess.run(cmd, capture_output=True, text=True, timeout=600)
  若 proc.returncode != 0:
    return PENALTY_SCORE  (1e6)

Step 5: 解析验证集 MAPE (CPU, < 10ms)
  result = ResultParser::parse(proc.stdout)
    - 正则提取: "MAPE_pv: <float>"
    - 正则提取: "MAPE_load: <float>"
    - 若解析失败: 返回 PENALTY_SCORE

Step 6: 计算加权分数 + 写入缓存
  score = 0.5 * MAPE_pv + 0.5 * MAPE_load
  cache[fingerprint] = score
  若 score < PENALTY_SCORE:
    记录为有效解
  返回 score

Step 7: 清理临时文件
  finally: os.unlink(temp_config.name)  // 确保训练脚本执行完毕后（无论成功/失败/超时）均删除临时配置文件
```

#### 15.4.3 失败处理与惩罚分数

| 场景 | 检测条件 | 返回值 | 处理 |
|------|----------|--------|------|
| 训练脚本崩溃 | `returncode != 0` | `PENALTY_SCORE = 1e6` | 标记该个体为"无效解"，不参与精英保留 |
| 训练超时 | `subprocess.TimeoutExpired` (默认 600s) | `1e6` | 同上 |
| 输出解析失败 | 正则未匹配到 MAPE 值 | `1e6` | 同上 |
| MAPE 异常（NaN/Inf） | `math.isnan(mape) or math.isinf(mape)` | `1e6` | 同上 |
| MAPE 超出合理范围 | MAPE > 100% | `1e6` | 同上（模型不收敛或数据问题） |

**无效解在种群中的处理：**
- 排序时无效解排在末尾（f = 1e6 >> 正常 MAPE 范围）
- 下一代更新时，无效解对应的个体被加入者和侦察者更新覆盖
- 记录无效解总数到 `search_metadata.invalid_solutions`

#### 15.4.4 评估缓存

**缓存文件：** `tools/mssa_optimizer/mssa_cache.json`

```json
{
  "cache_version": "1.0",
  "created": "2026-06-21T10:00:00",
  "training_data_fingerprint": "sha256-of-training-csv",
  "entries": {
    "a1b2c3d4e5f6a7b8": {
      "hyperparams": {
        "hidden_size": 64, "num_layers": 2, "attn_score": "additive",
        "vmd_k": 5, "vmd_alpha": 2000.0, "lr": 0.001,
        "batch_size": 32, "dropout": 0.2, "optimizer": "Adam", "input_window": 24
      },
      "mape_pv": 0.078, "mape_load": 0.121,
      "weighted_mape": 0.0995,
      "evaluated_at": "2026-06-21T10:05:30"
    }
  }
}
```

**缓存策略：**
- 缓存键 = 超参组合的 SHA256 前 16 位十六进制
- 跨运行持久化 -- 相同训练数据 + 相同超参 = 跳过训练
- `training_data_fingerprint` 校验 -- 训练数据变更时自动失效整个缓存
- 缓存大小预估：50 次迭代 * 30 个体 = 最多 1500 条记录 * ~200B = ~300KB

### 11.5 搜索空间映射

对应 PRD Section 3.5.2 的 10 维超参搜索空间：

| 维度 | 超参名 | 类型 | 编码方式 | 搜索范围 | 编码后表示 |
|------|--------|------|----------|----------|------------|
| 1 | `hidden_size` | 离散 | 整数索引 | {32, 64, 96, 128} | x[0] in [0, 3], 解码: values[floor(x[0]+0.5)] |
| 2 | `num_layers` | 离散 | 整数索引 | {1, 2, 3} | x[1] in [0, 2], 解码: values[floor(x[1]+0.5)] |
| 3 | `attn_score` | 枚举 | One-hot (3 维) | {additive, dot, general} | x[2:5] ∈ R^3, 解码: argmax |
| 4 | `vmd_k` | 整数 | 连续浮点 → 取整 | [2, 10] | x[5] ∈ [2, 10], 解码: clip(floor(x[5]+0.5), 2, 10) |
| 5 | `vmd_alpha` | 连续 | 浮点直接编码 | [100, 5000] | x[6] ∈ [100, 5000], 解码: clip(x[6], 100, 5000) |
| 6 | `lr` | log-连续 | log10 编码 | [1e-4, 1e-2] | x[7] ∈ [-4, -2], 解码: 10^x[7] |
| 7 | `batch_size` | 离散 | 整数索引 | {16, 32, 64, 128} | x[8] ∈ [0, 3], 解码: values[floor(x[8]+0.5)] |
| 8 | `dropout` | 连续 | 浮点直接编码 | [0.0, 0.5] | x[9] ∈ [0, 0.5], 解码: clip(x[9], 0, 0.5) |
| 9 | `optimizer` | 枚举 | One-hot (3 维) | {Adam, AdamW, RMSprop} | x[10:13] ∈ R^3, 解码: argmax |
| 10 | `input_window` | 离散 | 整数索引 | {12, 24, 36} | x[13] ∈ [0, 2], 解码: values[floor(x[13]+0.5)] |

**编码维度总计：** 离散(1+1+1+1=4) + One-hot枚举(3+3=6) + 连续/整数(1+1+1=3) = **13 维实际编码**，对应 **10 维逻辑超参**。

**解码器往返一致性：** `encode(decode(x)) == project(x)`（投影到有效超参空间），`decode(encode(h)) == h`。单元测试 `test_encode_decode()` 验证该性质。

**搜索空间配置化：** 每个超参的搜索范围和离散值集合可通过 `mssa_search_config.yaml` 覆盖（见 15.8），例如缩小 `vmd_alpha` 范围到 `[500, 3000]` 以加速搜索。

**舍入策略：** 统一使用 `floor(x + 0.5)` 代替 Python `round()`，确保 .5 边界始终向上舍入，消除银行家舍入（ties to even）可能引入的非确定性。

### 11.6 收敛与终止

对应 PRD Section 3.5.2 的三个终止条件：

| 序号 | 终止条件 | 配置项 | 默认值 | 判断逻辑 |
|------|----------|--------|--------|----------|
| 1 | 达到最大迭代次数 | `max_iterations` | 50 | `iteration >= max_iterations` |
| 2 | 连续无改善 | `no_improvement_rounds` | 10 | `stagnation_count >= no_improvement_rounds`，其中 `stagnation_count` 在 `|f_best_new - f_best_old| < epsilon` 时 +1，否则归零 |
| 3 | 总搜索时间超限 | `max_wall_time_seconds` | 7200 (2h) | `time.monotonic() - start_time >= max_wall_time_seconds` |

**收敛阈值：** `epsilon = 1e-4`，即加权 MAPE 改善小于 0.01 个百分点视为无改善。此阈值考虑 MAPE 测量的有效精度（通常在 0.1% 左右）、训练随机性（相同超参两次训练的 MAPE 波动约 0.05%-0.15%），epsilon 设为 0.01% 可过滤掉由训练随机性引起的虚假改善。

**终止时行为：**
- 若因条件 1 或 2 终止：`convergence_reason = "max_iter"` 或 `"no_improvement"`
- 若因条件 3 终止：`convergence_reason = "timeout"`，输出当前最优解
- 若因 Ctrl+C 终止：同 timeout 处理，`convergence_reason = "timeout"`

**收敛曲线：** `convergence_curve` 数组按迭代次序记录每次迭代后的全局最优目标函数值。收敛曲线单调不增（精英保留保证）。

**重启策略（非默认）：** 若 `convergence_reason = "no_improvement"` 且最优 MAPE > 人工基线 MAPE，运维可通过 `restart_on_stagnation: true` 配置自动重启动 MSSA（最多 3 次），每次使用不同的 `random_seed` 和佳点集偏移量。若 3 次重启后最优 MAPE 仍 > 人工基线 MAPE * 1.1，`quality_flag = "unusable"`，建议降级为 IPSO 或人工调参。

### 11.7 JSON 输出格式

严格对齐 PRD Section 7.4.2 JSON Schema。`output.py::SearchOutput::to_json()` 输出以下结构：

```json
{
  "search_metadata": {
    "algorithm": "MSSA",
    "start_time": "2026-06-21T10:00:00",
    "end_time": "2026-06-21T11:45:30",
    "total_iterations": 38,
    "convergence_reason": "no_improvement",
    "elapsed_seconds": 6330.5,
    "population_size": 30,
    "discoverer_ratio": 0.20,
    "scout_ratio": 0.10,
    "invalid_solutions": 3,
    "cache_hits": 187,
    "total_evaluations": 842
  },
  "best_hyperparameters": {
    "hidden_size": 64, "num_layers": 2, "attn_score": "additive",
    "vmd_k": 5, "vmd_alpha": 1800.0, "lr": 0.0015,
    "batch_size": 32, "dropout": 0.25, "optimizer": "Adam",
    "input_window": 24
  },
  "best_objective": {
    "weighted_mape": 0.0955, "mape_pv": 0.076, "mape_load": 0.115
  },
  "convergence_curve": [0.132, 0.124, 0.118, 0.112, 0.107, ...],
  "per_parameter_trajectory": {
    "hidden_size": [1, 1, ...],
    "num_layers": [1, 1, ...],
    "attn_score": [0, 0, ...],
    "vmd_k": [6, 6, 6, 5, 5, ...],
    "vmd_alpha": [2500, 2500, 2200, ..., 1800],
    "lr": [0.003, 0.003, 0.0025, ..., 0.0015],
    "batch_size": [2, 2, ...],
    "dropout": [0.3, 0.3, 0.25, ...],
    "optimizer": [0, 0, ...],
    "input_window": [1, 1, ...]
  },
  "quality_flag": "usable",
  "additional_info": {
    "population_size": 30, "discoverer_count": 6,
    "joiner_count": 21, "scout_count": 3,
    "opposition_learning_frequency": 5, "corsi_stagnation_threshold": 10,
    "corsi_initial_strength": 0.1, "corsi_decay_factor": 2.0,
    "random_seed": 42, "invalid_solutions": 3,
    "cache_hits": 187, "total_evaluations": 842,
    "stopped_early": false, "final_stagnation_count": 10,
    "final_diversity": 0.12
  }
}
```

**`per_parameter_trajectory` 值编码说明：**

| 超参 | 轨迹数组值含义 | 示例 |
|------|--------------|------|
| `hidden_size` | 离散选项索引 (0=32, 1=64, 2=96, 3=128) | `[1, 1, ...]` = 64 |
| `num_layers` | 离散选项索引 (0=1, 1=2, 2=3) | `[1, 1, ...]` = 2 |
| `attn_score` | One-hot argmax 索引 (0=additive, 1=dot, 2=general) | `[0, 0, ...]` = additive |
| `vmd_k` | 实际 K 值 (整数 2~10) | `[5, 5, ...]` = K=5 |
| `vmd_alpha` | 实际 alpha 值 (浮点) | `[1800.0, ...]` |
| `lr` | 实际学习率 (浮点) | `[0.0015, ...]` |
| `batch_size` | 离散选项索引 (0=16, 1=32, 2=64, 3=128) | `[2, 2, ...]` = 64 |
| `dropout` | 实际 dropout 率 (浮点) | `[0.25, ...]` |
| `optimizer` | One-hot argmax 索引 (0=Adam, 1=AdamW, 2=RMSprop) | `[0, 0, ...]` = Adam |
| `input_window` | 离散选项索引 (0=12, 1=24, 2=36) | `[1, 1, ...]` = 24 |

> **注（M-09 跟踪）：** 轨迹采用混合编码（连续型存储实际值，离散/枚举型存储索引），下游消费者需根据编码说明表区分哪些字段需二次解码。建议在实现阶段对 `per_parameter_trajectory` 同时提供 `_index` 和 `_value` 两个版本，或统一存储解码后的可读值。

**自校验：** `output.py::validate_output()` 在写入文件前自动执行以下校验：
1. `best_hyperparameters` 的所有必填键存在且类型正确
2. `best_objective.weighted_mape ≈ 0.5 * mape_pv + 0.5 * mape_load`（容差 1e-6）
3. `len(convergence_curve) == total_iterations`
4. `per_parameter_trajectory` 的每个键对应长度 = `total_iterations`
5. `convergence_reason` 为合法枚举值
6. `quality_flag` 为合法枚举值
7. 校验失败则写文件但标记 `quality_flag = "unusable"` 并记录 ERROR 日志

### 11.8 配置文件设计

**配置文件路径：** `tools/mssa_optimizer/config/mssa_search_config.yaml`

```yaml
# ============================================================================
# MSSA/IPS 超参搜索配置文件 (v1.0, 2026-06-21)
# 用于 MUPC 第三轮 MSSA/IPS 超参自动优化
# ============================================================================

# ---- 算法选择 ----
algorithm: "MSSA"             # "MSSA" (推荐) 或 "IPSO" (备选降级)
                              # IPSO 在搜索速度优先时使用，全局搜索能力弱于 MSSA

# ---- 随机种子（用于确定性复现）----
random_seed: 42               # 任意整数，相同 seed + 相同数据 → 确定性结果

# ---- 种群参数 ----
population:
  size: 30                    # 种群个体总数 [10, 100]。
                              # 若搜索空间较大或训练成本允许，可适当增大 N 至 50~60 以提升全局搜索能力
  discoverer_ratio: 0.20      # 发现者比例 [0.10, 0.30]
  scout_ratio: 0.10           # 侦察者比例 [0.05, 0.20]
  # 加入者比例 = 1 - discoverer_ratio - scout_ratio (自动计算)

# ---- 增强策略（仅 MSSA 生效，IPSO 忽略）----
enhancement:
  good_point_set: true        # 佳点集初始化
  opposition_learning: true   # 反向学习增强
  opposition_frequency: 5     # 反向学习执行频率（每 N 次迭代）
  corsi_mutation: true        # Corsi 变异扰动
  corsi_stagnation: 10        # 触发 Corsi 的停滞阈值（连续无改善次数）
  corsi_strength: 0.1         # Corsi 变异初始强度 C_0
  corsi_decay: 2.0            # Corsi 衰减因子 beta

# ---- 终止条件 ----
termination:
  max_iterations: 50          # 最大迭代次数
  no_improvement_rounds: 10   # 连续无改善轮次（|delta_MAPE| < epsilon）
  epsilon: 1.0e-4             # 收敛阈值（加权 MAPE 绝对改善）
  max_wall_time_seconds: 7200 # 总搜索时间硬上限 (2 小时)
  restart_on_stagnation: false  # 停滞时是否自动重启（最多 3 次）[默认 false]

# ---- 目标函数 ----
objective:
  pv_weight: 0.5              # 光伏 MAPE 权重
  load_weight: 0.5            # 负荷 MAPE 权重
  penalty_score: 1000000.0    # 训练失败惩罚分数
  training_timeout_seconds: 600  # 单次训练超时 (10 分钟)
  cache_enabled: true         # 是否启用评估缓存
  cache_path: "mssa_cache.json"  # 缓存文件路径（相对 mssa_optimizer/）

# ---- 训练脚本调用 ----
training:
  script_path: "../../train.py"  # 训练脚本路径（相对 mssa_optimizer/ 或绝对路径）
  python_executable: null      # Python 解释器 (null = sys.executable)
  extra_args: []               # 额外命令行参数 (如 ["--gpu", "0"])

# ---- 搜索空间覆盖（可选，用于缩小搜索范围）----
# 未指定的超参使用 PRD Section 3.5.2 的默认搜索范围
search_space_overrides:
  # hidden_size: [32, 64, 96, 128]           # 离散值列表
  # num_layers: [1, 2, 3]                     # 离散值列表
  # attn_score: ["additive", "dot", "general"] # 枚举值列表
  # vmd_k: {min: 2, max: 10}                  # 整数范围
  # vmd_alpha: {min: 100, max: 5000}           # 连续范围
  # lr: {min: 1.0e-4, max: 1.0e-2}            # 连续范围（线性空间，内部 log 编码）
  # batch_size: [16, 32, 64, 128]             # 离散值列表
  # dropout: {min: 0.0, max: 0.5}             # 连续范围
  # optimizer: ["Adam", "AdamW", "RMSprop"]    # 枚举值列表
  # input_window: [12, 24, 36]                # 离散值列表

# ---- 输出 ----
output:
  result_path: "mssa_search_result.json"  # 搜索结果输出路径
  verbose: true                            # 是否输出每次迭代的详细日志
  log_level: "INFO"                        # 日志级别: DEBUG / INFO / WARNING / ERROR
```

**配置校验规则（`config.py::validate()`）：**

| 校验项 | 条件 | 失败处理 |
|--------|------|----------|
| `algorithm` | 必须为 `"MSSA"` 或 `"IPSO"` | 拒绝执行 |
| `population.size` | `10 <= size <= 100` | 拒绝执行 |
| `discoverer_ratio + scout_ratio` | `< 1.0` | 拒绝执行（加入者数量 = 0 不可接受） |
| `max_iterations` | `>= 1` | 拒绝执行 |
| `max_wall_time_seconds` | `>= 300` (至少 5 分钟) | 拒绝执行 |
| `pv_weight + load_weight` | `≈ 1.0` (容差 1e-6) | 自动归一化并记录 WARN |
| `penalty_score` | `> max_possible_mape` (即 > 1.0) | 使用默认值 1e6 |
| `training_timeout_seconds` | `>= 60` | 使用默认值 600 |
| `training.script_path` | 必须指向存在的文件 | 拒绝执行 |
| 搜索空间覆盖值 | 每个超参须合法（离散值 >= 2 个选项、连续值 min < max） | 拒绝执行 |

**IPSO 降级路径配置切换：**

当 `algorithm: "IPSO"` 时：
- `enhancement.*` 全部忽略（IPSO 不使用佳点集、反向学习、Corsi 变异）
- `population` 参数沿用（IPSO 同样使用种群概念）
- 输出 JSON 中 `search_metadata.algorithm = "IPSO"`，`additional_info` 中不含 MSSA 特有的 `opposition_learning_frequency`、`corsi_*` 等字段
- 其余 JSON Schema 完全一致（PRD Section 7.4.2 兼容）


