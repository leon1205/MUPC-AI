# 下游 AI 引擎文档不一致待处理清单

> **用途**：本文件记录训练管线项目 (MUPC-AI2) 在与下游 AI 引擎项目对齐过程中，发现的下游文档自身不一致点。这些点需要下游团队澄清/修复。
> **维护人**：MUPC-AI2 项目组
> **当前版本**：2026-06-17 (下游 v2.15 同步后)

---

## 待下游澄清项

### 1. SCENE-B3 容量/里程价格配置项缺失 (待处理)

**问题描述**：
- 下游 PRD v2.15 Section 7.5 写 `R_ancillary_service = P_regulation_capacity·capacity_price + P_regulation_mileage·mileage_price`
- 下游 PRD/设计文档未提供 `capacity_price` / `mileage_price` 的具体数值或配置字段
- 本地训练管线使用硬编码占位值（capacity=0.1 元/kW, mileage=0.05 元/kW），已配置化为 `cfg.vpp_pricing`

**建议下游处理**：
- 在 FusedSystemState 或 `dispatch_p_set` 关联字段中提供 capacity_price / mileage_price
- 或在配置文档 (06-训练管线-动态配置系统说明.md) 中明确定义默认值

---

### 2. SCENE-B1 P_battery_degradation 公式形式差异 (待处理)

**问题描述**：
- 下游 PRD v2.15 Section 7.3 公式：`P_battery_degradation = β·Σ(|P_batt_set|·Δt)/E_battery_total·100`
- 本地训练管线实现：C-rate² = (|p_batt|/BATTERY_CAPACITY)²
- 数值上**不等价**：下游是累积绝对能量（健康度损失），本地是 C-rate²（瞬时应力）

**本地处理策略**：训练实现采用 C-rate² 简化形式（瞬时应力，与训练 RL 优化的常见做法一致），部署侧 Rust 实现按下游 PRD 公式（累积能量）。

**建议下游处理**：
- 在下游 PRD 中明确"训练/部署实现可采用简化近似，但物理量纲需对齐"
- 或在 Section 7.3 提供 C-rate² 简化形式作为训练侧等价公式

---

## 已处理的下游不一致（历史）

### ✅ #1 SafetyOverride reason_code 字段不一致 (2026-06-17 下游 v2.15 修复)

**原始问题**：下游 v2.14 已删除 `safety_override_reason_code` 字段（D9 4 字段：active/p_ref/consecutive/ratio），但 v2.15 PRD 7.2 仍按 4 个原因差异化。

**下游 v2.15 处理**：
- 删除 `match reason { voltage_violation/q_exhausted/emergency/... }` 分支
- 样本不足时统一使用固定惩罚 -3.33（≈ 原 voltage_violation -50/15 档位）
- 样本充足时维持 `(-5·ratio - 10·(consecutive/10).clamp(0,1))/15`
- 设计文档新增 `cold_start_penalty = 3.33` 配置项

**本地训练管线验证**：
- `_compute_safety_override()` 实现：`consecutive<10 → -50/15 ≈ -3.333`, `consecutive≥10 → (-5·ratio - 10·(consecutive/10).clamp(0,1))/15`
- 与下游公式完全一致（-3.33 与 -50/15 ≈ -3.333 数值差异 ~0.1%，可接受）

---

**最后更新**：2026-06-17 by Claude Code (下游 v2.15 同步后)